# 统一组合与手工下单系统

## 目标

三条策略不会各自输出互相冲突的“买入建议”，而是先进入同一个组合层。组合层只生成可审计的人工执行清单，不连接券商，也不宣称订单已经成交。

## 决策顺序

```text
ETF 轮动 / 股票反转 / 可转债双低候选
  -> 市场状态与策略样本外验证
  -> 动态策略预算
  -> 标的级确定性风险闸门
  -> 单标的与总敞口限制
  -> 整手数量、最低交易金额、现金储备
  -> 卖单优先的手工下单清单
```

LLM 和多 Agent 可以提供研究证据，但不能绕过这一层的硬约束。

同一交易日存在已保存的多 Agent 决策时，组合层执行单向软闸门：

- `buy/add`：只能把策略目标限制在 Agent 目标权重以内；
- `hold/watch`：禁止对空仓标的新增，已有仓位不能因 Agent 结论而增加；
- `reduce/sell`：降低或清零策略目标；
- `review_required`：新增仓位进入人工复核。

Agent 永远不能提高硬风控、策略预算或单标的上限。

## 策略预算

动态预算使用样本外 Sharpe、Calmar、近期收益、最大回撤、数据质量和市场状态。没有注册 walk-forward 结果的策略被限制在配置下限附近。策略除至少 3 个测试折、正收益折比例不低于 50%、平均 Sharpe 为正且最差回撤优于 -25% 外，还必须通过 ETF 等权基准闸门，才允许提高预算。

当前主动 ETF 轮动没有冻结且准入的生产参数，因此订单预算为0；显式选择也只能进入研究复核。可转债和未准入A股同样不会通过配置下限获得隐含预算。

ETF walk-forward 与生产核心协议验证分别保存在 `strategy_validations`。预测模型的 Brier/Log Loss 准入与策略收益验证是两套不同的治理流程，不能互相冒充。

## 股票风险闸门

股票新仓必须完成：

- ST/*ST、退市、停牌、涨停与监管调查检查；
- 股权质押、限售解禁、诉讼仲裁检查；
- 新浪和同花顺财务数据交叉验证；
- 自由现金流、利息覆盖、现金利润比等永久损失检查。

风险数据或财务数据未完成时状态为 `review_required`。确定性财务否决、ST、质押率不低于 50% 等情况为 `blocked`。

## 可转债风险闸门

系统检查价格、转股溢价率、信用评级、到期日、规模和强赎风险。免费源暂时无法稳定给出“剩余规模”时，候选只能进入人工复核，不能自动成为可执行清单。

## 输出状态

- `actionable`：通过当前规则，可供用户再次核对后手工下单。
- `blocked`：触发硬否决，目标新增仓位为 0。
- `review_required`：关键证据缺失或冲突，需要人工补充。
- `below_minimum`：预算不足以购买一手，或交易金额低于最小值。

每次保存的计划包含上一版管理目标。某个已管理标的离开新目标集后，系统会生成退出清单；不会擅自卖出从未纳入系统管理的用户持仓。

默认 ETF 核心不使用未经回测的 ATR 止损，而是严格使用半年调仓和2%偏离容差。ATR只保留在研究型主动轮动候选中，不能进入默认核心订单。

## 当前边界

- 手工交易账本的持仓价格默认是最后一次录入成交价，尚未自动盯市。
- 股票点时成分股和退市股历史仍不完整，因此股票策略暂不能注册无幸存者偏差的 walk-forward 结果。
- 免费可转债源可能失败或缺少剩余规模、强赎连续天数等字段。
- 任何 `actionable` 仍需在下单前重新确认价格、停牌、涨跌停和公告。
- 交易计划要求行情日期不超过1个工作日；更陈旧的数据触发硬否决，而不是普通文字警告。
