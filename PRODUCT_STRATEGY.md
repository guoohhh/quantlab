# QuantLab 产品定位与功能需求

> 本文是 QuantLab 的产品意图权威。它定义服务对象、核心价值、非目标、权限边界和验收合同；
> 不负责记录 Runtime、Provider、数据库、测试或正式样本的当前状态。实现是否存在以代码和测试为准，
> 动态状态按 `docs/AI_HANDOVER.md` 核验。

## 1. 一句话定位

QuantLab 是面向普通投资者的 AI 辅助研究与模拟交易平台，提供市场趋势、资金流向、个股研究、组合风险检查和完整模拟交易。AI 提供证据与建议，用户保留最终决策权。

对外短句：

> 看懂市场，辅助决策，模拟验证。

项目不应再把“多 Agent 数量”“模型数量”或“策略数量”作为首要卖点。多 Agent、量化策略、回测和审计是底层能力，用户首先看到的应是：

- 今天市场处于什么状态；
- 哪些标的值得进一步研究；
- 买入、加仓、减仓或继续观察的依据是什么；
- 一笔操作会怎样改变账户和组合风险；
- 用户过去的模拟决策最终产生了什么结果。

## 2. 目标用户

### 2.1 普通投资者和投资小白

这里的“小白”描述的是使用门槛，而不是教学目标。

他们需要：

- 不必理解量化、Agent、Brier Score 等底层概念也能开始使用；
- 可以搜索股票、查看市场和模拟下单；
- 在操作前看到清楚的证据、风险和账户影响；
- 在操作后看到持仓、盈亏和原始判断是否兑现。

产品不要求用户先完成课程、问卷或新手任务。

### 2.2 已有投资经验的个人投资者

他们需要：

- 更高效地汇总行情、趋势、资金、财务、事件和多空证据；
- 检查已有持仓和新订单的集中度、相关性及最大计划损失；
- 使用 AI 作为研究副驾驶，而不是自动交易员；
- 保存当时的决策依据，方便后续复盘；
- 将模拟结果与简单基准和原始策略进行比较。

## 3. 明确不做什么

### 3.1 不做投资教学平台

第一版不建设：

- 系统化课程；
- 章节、学习进度和考试；
- 投资知识打卡；
- 面向新手的强制教学流程；
- 以知识库阅读量为核心的产品指标。

界面仍需解释影响当前操作的必要信息，例如“资金流入不等于未来上涨”“买入后单股仓位将达到 22%”。这属于决策解释，不属于教学系统。

### 3.2 不做强制风险画像

第一版不要求用户完成风险承受能力问卷，也不根据人格或偏好自动放宽风控。

系统使用统一、透明的交易安全规则：

- 单标的和总仓位上限；
- 行业集中度；
- 数据新鲜度；
- ST、停牌、退市和重大风险事件；
- A 股整手、T+1、涨跌停和费用；
- 最大计划损失和现金储备。

未来如增加“稳健/均衡/进取”，它只能改变展示排序和软建议，不能放宽数据、合规和确定性硬闸门。

### 3.3 不连接券商自动交易

第一版只支持：

- 用户自主模拟交易；
- AI 辅助生成操作建议；
- 真实交易由用户在外部券商人工执行；
- 用户可手工登记真实成交，供组合跟踪和复盘。

### 3.4 不承诺收益

回测、历史盲测、资金流、Agent 观点和概率预测都不能转换成收益保证。主动策略、统计模型和 LLM 必须分别证明增量价值。

## 4. 产品原则

1. **先给结论，再展开证据。** 首页先回答今天是否需要行动，再允许用户进入因子、Agent 和审计细节。
2. **AI 提建议，用户做决定。** AI 不能未经确认自动下单，也不能绕过硬风控。
3. **模拟交易遵守真实规则。** 不能使用无限流动性、零成本或当天买入当天卖出等不真实假设。
4. **不为了演示强行推荐。** `watch`、`hold`、`review_required` 和空候选都是正常结果。
5. **资金流是证据，不是答案。** 任何资金流指标都不能单独生成买入结论。
6. **专业复杂度留在系统内部。** 普通用户不需要先理解模型、Agent 和统计术语。
7. **失败和降级必须可见。** 数据缺失、行情陈旧、模型失败和策略未准入必须展示。
8. **每笔决策可以回到当时。** 保存下单前看到的数据、AI 建议、风险检查和用户最终选择。

## 5. 建议的信息架构

产品层固定为五个主入口；账户、通知、设置和帮助中心属于工具入口，不与决策流程并列。

### 5.1 首页

- 市场状态和风险偏好；
- 模拟账户总资产、今日盈亏和累计盈亏；
- 当前仓位和现金；
- 今日需要关注、复核或处理的事项；
- 持仓风险变化；
- 最近 AI 观点变化；
- 快速进入搜索、研究和下单。

### 5.2 行情与发现

- 市场温度；
- 资金流向；
- 行业趋势；
- 股票和 ETF 搜索；
- 系统研究候选；
- 自选列表；
- 资金、价格和事件异动。

### 5.3 AI 研究

- 个股或 ETF 研究；
- 量价、财务、事件和资金证据；
- 多空观点；
- 概率预测及其证据等级；
- Reviewer 结论；
- 建议动作和建议数量；
- 买入后的组合影响；
- 可复算轨迹和审计报告。

### 5.4 模拟交易

- 模拟账户；
- 委托下单；
- 持仓和盈亏；
- 加仓、减仓和清仓；
- 委托、成交和撤单记录；
- 账户净值曲线；
- 与基准比较；
- 决策和交易复盘。

### 5.5 我的与高级设置

- 自选分组和预警；
- 手工登记的真实交易；
- 数据源状态；
- LLM 模型和角色路由；
- 审计报告；
- 历史盲测、策略实验室、学习状态等研究模式入口。

多 Agent 圆桌、证据中心、策略实验室、模型训练和 LLM 配置应保留，但默认放入高级研究模式，不与普通用户的每日操作并列。

## 6. 核心用户流程

### 6.1 普通用户模拟交易

```text
打开产品
→ 查看市场和资金趋势
→ 搜索或选择研究候选
→ 查看 AI 研究与交易前检查
→ 输入模拟买入数量或金额
→ 查看买入后的现金、仓位和最大计划损失
→ 用户确认
→ 订单按真实交易规则进入委托和成交
→ 持仓按最新行情更新盈亏
→ 用户加仓、减仓或清仓
→ 查看交易结果和原始判断是否兑现
```

该流程不要求先完成风险问卷或课程。

### 6.2 已有投资者使用 AI 辅助决策

```text
搜索持仓或新标的
→ 冻结当前行情、资金、财务和事件证据
→ AI 形成多空观点与风险检查
→ 系统计算加入该标的后的组合变化
→ 给出建议动作、数量区间和失效条件
→ 用户自主决定是否在外部券商下单
→ 可选地手工登记真实成交
→ 系统继续跟踪观点变化和组合风险
```

## 7. 用户自主模拟交易需求

### 7.1 与现有模拟盘的区别

现有模拟盘主要验证系统策略和 Agent 的前瞻表现，属于不可回写的影子账户。

下一阶段需要新增“用户模拟账户”：

| 类型 | 目的 | 谁决定交易 | 是否允许自由操作 |
|---|---|---|---|
| 系统影子账户 | 验证策略和 Agent 增量 | 固定系统规则 | 否 |
| 用户模拟账户 | 让用户体验完整投资过程 | 用户本人 | 是 |
| 手工真实账本 | 跟踪外部券商成交 | 用户本人 | 只登记，不代下单 |

三类账户必须隔离，不能用用户自由交易结果污染策略前瞻证据。

### 7.2 模拟账户

第一版至少支持：

- 创建一个或多个模拟账户；
- 默认初始资金 100,000 元，可自定义；
- 账户名称、创建时间、状态；
- 总资产、可用现金、持仓市值；
- 今日盈亏、累计盈亏和累计收益率；
- 最大回撤；
- 股票、ETF 和现金占比；
- 沪深300或用户选择的基准；
- 账户重置或开启新赛季，但历史记录不可静默覆盖。

### 7.3 模拟订单

支持以下动作：

- 买入新标的；
- 对已有持仓加仓；
- 部分减仓；
- 全部清仓；
- 撤销未成交委托。

订单状态至少包括：

- `pending`：等待可成交时间；
- `partially_filled`：部分成交；
- `filled`：全部成交；
- `cancelled`：用户撤单；
- `rejected`：违反规则或条件不允许；
- `expired`：超过有效期。

每个拒绝状态必须显示具体原因，不能只返回“交易失败”。

### 7.4 成交规则

模拟交易应复用现有确定性交易规则，并补齐用户委托生命周期：

- A 股买入数量必须为 100 股整手；
- 当日买入股票次一交易日才可卖；
- 停牌不可成交；
- 一字涨停不能假设买入；
- 一字跌停不能假设卖出；
- 非交易时间下单进入下一个可执行交易日；
- 支持佣金最低收费、印花税、过户费和滑点；
- 成交使用原始价格，研究信号使用正确的复权价格；
- 数据陈旧时不得生成看似真实的当日成交；
- 第一版不必模拟盘口排队，但必须在界面标注成交假设。

### 7.5 持仓与盈亏

每个持仓展示：

- 证券名称和代码；
- 数量、可卖数量和冻结数量；
- 平均成本；
- 最新价格和行情时间；
- 持仓市值；
- 今日盈亏、累计盈亏和收益率；
- 仓位占比；
- 已实现和未实现盈亏；
- 首次买入原因和关联研究报告；
- 当前 AI 观点及相对买入时的变化；
- 原决策失效条件是否触发。

### 7.6 账户曲线和对照

至少提供：

- 总资产净值曲线；
- 现金与仓位变化；
- 每日盈亏；
- 最大回撤；
- 与沪深300、ETF 等权或用户选定基准对比；
- 费用累计；
- 换手率和交易次数。

避免只展示绝对赚亏。用户需要知道收益是否只是来自同期市场整体上涨。

### 7.7 交易前 AI 检查

用户提交模拟订单前，系统生成简洁的检查卡：

- 当前趋势和资金状态；
- 关键财务、事件和数据质量；
- AI 多空结论；
- Reviewer 是否通过；
- 输入订单后的单股、行业和总仓位；
- 可用现金变化；
- 止损参考或失效条件；
- 在假设下跌 10%/15% 时的账户损失；
- 建议数量、建议区间或不建议操作的原因。

AI 建议不能替代用户确认。即使用户忽略软建议，系统也应保存“系统建议”和“用户最终操作”的差异；硬闸门仍然不能被绕过。

### 7.8 决策与交易复盘

复盘不是教学模块，而是账户功能的一部分。它回答：

- 当时为什么操作；
- 用户是否采纳 AI 建议；
- 持有期间最大浮盈和浮亏；
- 原始风险或失效条件是否出现；
- 当前实际收益；
- 与同期基准相比的结果；
- AI 当时的概率和动作是否兑现。

第一版以客观事实为主，不输出“你应该学习某课程”之类的教学建议。

### 7.9 建议的数据模型

可在现有 SQLite 账本基础上增加或明确以下实体：

- `user_paper_accounts`：用户模拟账户；
- `user_paper_orders`：用户委托；
- `user_paper_fills`：模拟成交；
- `user_paper_positions`：当前持仓；
- `user_paper_equity_snapshots`：每日账户快照；
- `user_trade_decision_links`：订单与研究报告、AI 检查的关联；
- `user_trade_reviews`：平仓或阶段性复盘。

关键要求：系统影子账户和用户模拟账户必须有明确的 `account_type` 或独立表，防止证据污染。

### 7.10 建议的 API

- `POST /api/simulator/accounts`
- `GET /api/simulator/accounts`
- `GET /api/simulator/accounts/{id}`
- `POST /api/simulator/accounts/{id}/orders`
- `DELETE /api/simulator/accounts/{id}/orders/{order_id}`
- `GET /api/simulator/accounts/{id}/orders`
- `GET /api/simulator/accounts/{id}/positions`
- `GET /api/simulator/accounts/{id}/equity-curve`
- `POST /api/simulator/accounts/{id}/settle`
- `GET /api/simulator/accounts/{id}/reviews`

所有写接口都必须启用本地令牌认证或严格限制在回环地址。

## 8. 资金流向页面需求

### 8.1 产品目的

资金流向页面用于回答：

- 当前市场的交易热度和风险偏好如何；
- 资金正在集中于哪些行业或资产；
- 资金趋势是否持续；
- 资金变化是否得到价格和成交量确认；
- 某只股票的资金表现相对行业处于什么位置。

它不直接回答“应该买哪只股票”。

### 8.2 数据口径原则

“主力资金”“大单净流入”等指标通常由成交方向和订单大小推算，不代表已确认的机构真实持仓变化。页面必须展示：

- 数据来源；
- 更新时间；
- 指标定义；
- 是否为估算值；
- 当前数据是否缺失或降级；
- 不同数据源口径不可直接混用的提示。

如果可靠数据不足，宁可显示“暂不可用”，不能用合成数据冒充真实资金流。

### 8.3 全市场资金温度

建议展示：

- 沪深两市成交额；
- 相对 5 日和 20 日均值的放量/缩量；
- 上涨、下跌和平盘家数；
- 涨停和跌停数量；
- 市场宽度；
- 融资余额变化（如数据可得）；
- 股票、债券、黄金、海外和现金类 ETF 的相对强弱；
- ETF 份额或净申购变化（如数据可得）；
- 市场资金风险偏好摘要。

摘要示例：

> 当前成交额放大，但上涨集中于少数成长行业，市场宽度一般，属于局部风险偏好上升。

### 8.4 行业资金流向

行业层应优先于个股层建设，因为数据更稳定、用户更容易理解。

每个行业展示：

- 今日、3 日、5 日和 20 日资金趋势；
- 成交额和换手变化；
- 行业内上涨家数比例；
- 行业价格趋势；
- 资金与价格是否一致；
- 资金是否只集中在少数龙头；
- 拥挤度或过度延伸提示；
- 代表性股票和 ETF。

建议的状态标签：

- `量价共振`：资金和价格同向且行业宽度改善；
- `流入未确认`：资金流入但价格或宽度未确认；
- `价格资金背离`：价格上涨但资金持续流出；
- `短期异动`：单日明显变化但缺乏持续性；
- `数据不足`：不能形成可靠判断。

### 8.5 个股资金流向

个股页可展示：

- 今日、3 日、5 日和 20 日净流向；
- 大单、中单和小单结构；
- 成交额变化；
- 换手率；
- 资金与价格背离；
- 所属行业相对排名；
- 与历史自身分位比较；
- 数据质量和更新时间。

个股资金流必须与以下证据共同出现：

- 价格趋势；
- 行业趋势；
- 波动率和过度延伸；
- 流动性；
- 财务和事件硬风险。

### 8.6 资金流不能拥有的权限

- 不能单独生成 `buy`；
- 不能绕过 Reviewer；
- 不能绕过数据新鲜度；
- 不能放宽单股或总仓位上限；
- 不能因为单日大额流入自动提高订单数量；
- 不能把供应商的“主力”标签描述成已确认机构行为。

资金流更适合作为研究候选排序、证据摘要和预警输入。

### 8.7 建议的交互

页面从上到下：

```text
市场资金温度
→ 资产与行业流向热力图
→ 持续流入/流出行业
→ 资金价格背离
→ 用户自选和持仓资金变化
→ 点击进入个股 AI 研究或模拟下单
```

默认突出持续性和相对变化，而不是只展示绝对净流入排行榜。

### 8.8 资金流向验收标准

- 所有数字有来源和时间戳；
- 支持至少今日、5 日和 20 日三个周期；
- 行业流向可与行业价格趋势同时查看；
- 单一数据源失败时显式降级；
- 不可用时不补造结果；
- 点击行业可进入成分或代表标的；
- 点击个股可进入 AI 研究；
- 资金流不会直接产生订单；
- 页面提供一段可复算的资金与价格一致性说明。

## 9. AI 辅助下单需求

AI 辅助下单不是“AI 帮用户点买入”，而是把研究结论转换成可理解、可检查的操作方案。

输出至少包括：

- 建议动作：观察、持有、买入、加仓、减仓、清仓或复核；
- 建议数量或数量区间；
- 参考价格和行情时间；
- 主要支持证据；
- 主要反对证据；
- 失效条件；
- 数据缺口；
- 买入后的账户和组合影响；
- 当前证据等级；
- 用户需要最终确认的事项。

对于已有真实持仓，AI 可以辅助生成外部券商的手工操作清单；对于用户模拟账户，可以预填模拟委托，但仍需用户确认。

## 10. 组合管理需求

产品不应只推荐更多股票，还应帮助用户理解现有组合。

组合页至少回答：

- 总仓位和现金；
- 单股和行业集中度；
- 高相关持仓；
- 股票、ETF、债券、黄金等资产分布；
- 今日和累计盈亏来源；
- 最大计划损失；
- 新增某标的后的风险变化；
- 哪些持仓的原始决策条件已发生变化；
- 哪些操作因数据、规则或风险被阻止。

第一版可提供透明情景：

- 个股下跌 10%/15%；
- 所属行业下跌 10%；
- 大盘下跌 10%；
- 两天无法卖出；
- 多个高相关持仓同步下跌。

这些是确定性压力假设，不应包装成未来预测。

## 11. 当前产品合同

当前代码应提供以下相互连接、但权限分离的产品表面：

| 产品表面 | 用户价值 | 不拥有的权限 |
|---|---|---|
| 今日 | 聚合资产、持仓、风险、提醒和待办 | 为了活跃度强行推荐 |
| 市场与发现 | 搜索、自选、市场/行业/资金线索和候选 | 用资金代理直接生成买单 |
| 研究台 | 冻结研究、正反证据、引用、概率、Chat 和圆桌 | 修改事实、硬风控或自动成交 |
| 组合与交易 | 用户模拟账户、交易前检查、确认、订单、成交和净值 | 绕过服务器行情与用户确认 |
| 决策复盘 | 连接论文、研究、订单、费用、结果与 Reflection | 用事后结果改写原始判断 |
| 专业空间 | 任务、通知、只读外部账本、Runtime、数据和模型设置 | 冒充券商连接或正式投资证明 |
| 帮助中心与全局助手 | 解释当前任务、错误、规则和证据 | 用说明文字替代真实状态 |

导航层级固定区分五个一级决策工作区、工具入口和二级路由。移动端、桌面端和折叠导航必须保持
同一信息架构；详情页和工具不能不断膨胀成新的一级入口。

上述“应提供”是产品合同，不是当前运行声明。外部验收仍需从当前代码、测试和隔离浏览器流程确认。

## 12. 当前优先级

### P0：黑客松现场可靠性与叙事

1. 保持一键隔离 Demo 可在无 API Key、无外部行情时完整运行；
2. 三分钟内走完“发现 → 冻结研究 → 风控 → 用户确认 → 模拟成交 → 复盘”；
3. 桌面和窄屏不遮挡关键动作，失败时有明确回退；
4. 现场只演示可验证能力，不把历史收益、恢复样本或 Mock 数据包装成实时 Alpha；
5. 让评委看见 QuantLab 的差异是证据、反证、权限和复盘，而不是 Agent 数量。

### P0：最终验收与动态事实一致性

- 让 `docs/AI_HANDOVER.md` 成为外部 AI 的单一接管入口；
- 保持代码、Schema、API、测试和文档对五入口与账户隔离的描述一致；
- Provider、Runtime、readiness、Primary 和样本状态只从机器证据生成；
- 对首次自然窗口、恢复运行、backfill 和 Historical Demo 保持清楚标签；
- 任何不满足的金融 readiness 继续 fail closed。

### P1：自然前瞻证据

- 按冻结协议积累不可回写的 5/20 交易日样本；
- 持续比较纯量化、LLM、统计融合和完整系统；
- 使用含成本组合、可投资基准、回撤、置信区间和跨状态表现；
- 不为改善结果修改旧账户、阈值或样本身份。

### P1：数据与日常可用性

- 提高许可清晰的数据覆盖与 Provider 观测性；
- 在行情、资金、行业或事件不足时展示缺失原因和最近成功时间；
- 完成真实多日运行、备份恢复和通知投递观测；
- 保持免费数据无 SLA、日线不等于执行报价的边界。

### P2：受证据驱动的扩展

只有现有链路和自然证据稳定后，才考虑新的策略、开放权重模型、桌面打包、MCP/Skill 或更广平台化。
券商自动交易、多租户 SaaS 和收益承诺不属于通过黑客松而应扩张的范围。

## 13. 模拟交易验收合同

- 用户可创建 10 万元模拟账户；
- 可搜索股票并提交买入委托；
- 买入前显示 AI 研究、行情时间和组合影响；
- 100 股整手、T+1、费用、停牌和涨跌停规则生效；
- 用户可加仓、减仓、清仓和撤销未成交委托；
- 持仓自动显示成本、最新价、盈亏和仓位；
- 账户展示现金、总资产、净值曲线和最大回撤；
- 可与沪深300基准比较；
- 数据陈旧时不生成虚假当日成交；
- 每笔订单可追溯到当时研究和用户确认；
- 用户自由交易不会进入策略验证成绩单；
- 重启应用后账户、委托、成交和持仓保持一致。

## 14. 产品成功指标

第一阶段不以课程完成率或测试分数衡量。建议关注：

- 完成首次模拟下单的用户比例；
- 从股票发现进入 AI 研究的比例；
- 从 AI 研究进入模拟委托的比例；
- 用户每周查看持仓和盈亏的次数；
- 有多少订单在确认前被用户调整数量或取消；
- 数据/风险闸门阻止了多少不合格订单；
- 订单与研究报告关联完整率；
- 模拟账户与基准的比较覆盖率；
- 用户在平仓后查看复盘的比例；
- 资金流页面进入个股研究的转化率。

这些指标衡量产品是否帮助用户完成更清楚、更可复核的决策，而不是诱导更高交易频率。

## 15. 黑客松演示主线

演示不要遍历所有 Agent 和页面。建议三幕：

1. **看市场：** 展示市场温度和资金正在流向哪些行业；
2. **做决策：** 选择一只股票，展示 AI 研究、反对证据和买入后的组合影响；
3. **模拟验证：** 用户确认模拟下单，展示成交规则、持仓盈亏和之后可复盘的决策快照。

最后补充证据中心：主动策略和 LLM 没有通过的证据也会保留，系统不会为了演示强行给出买入。

建议对外表达：

> QuantLab 不是让 AI 更自信地荐股，而是让投资者在行动前看到更完整的证据，在行动后能够用真实结果验证自己的判断。

## 16. 最终产品边界

QuantLab 的核心不是教学、自动交易或承诺收益，而是：

- 让普通投资者低门槛地查看市场和研究标的；
- 让用户在真实约束下完成模拟买卖；
- 让 AI 在操作前提供证据、反证和组合影响；
- 让每笔决策在操作后可以用盈亏和基准验证；
- 让复杂模型、Agent 和量化研究服从用户可理解、可控制、可审计的产品流程。
