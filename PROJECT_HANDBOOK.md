# QuantLab 项目手册

本手册描述面向用户的稳定产品概念，不保存容易过期的数据库行数、Provider 探针结果、测试总数
或某一天的进程状态。实时事实请运行 `quantlab runtime-status` 并核对生产数据库与最新机器报告。

## 产品定位

QuantLab 服务于希望自己做最终决定的个人投资者。系统帮助用户发现候选、组织证据、看见分歧、
评估组合影响、完成模拟交易并复盘判断，但不会代替用户连接券商自动买卖。

产品的核心不是“更多 Agent”，而是让一次判断具备完整身份：标的、数据截至时间、来源、支持证据、
反对证据、失效条件、模型意见、确定性风控、用户决定和后续结果都能重新找到。

## 使用路径

1. 在 `今日` 查看资产、风险、提醒和待办；
2. 在 `市场与发现` 搜索标的、观察市场与资金线索；
3. 在 `研究台` 打开冻结研究，查看量化信号、多 Agent 分歧和证据引用；
4. 必要时进入独立的 `专家圆桌`，观察逐轮讨论，而不只看最终结论；
5. 在 `组合与交易` 运行服务器侧交易前检查，由用户确认模拟订单；
6. 在 `决策复盘` 对照当时判断、成交费用、盈亏和失效条件；
7. 在 `专业空间` 管理通知、后台任务、外部只读账本、模型与邮件设置；
8. 不熟悉流程时随时使用右侧全局 AI 助手或 `帮助中心`。

## 权限边界

| 层级 | 负责什么 | 不能做什么 |
|---|---|---|
| 数据与证据 | 来源、时间、新鲜度、缺失原因、点时身份 | 用未来数据补写历史 |
| 确定性策略与风控 | 指标、费用、仓位、T+1、涨跌停、订单规则 | 被 LLM 绕过 |
| LLM 与多 Agent | 解释、反证、概率、研究草稿、圆桌讨论 | 伪造事实或自动确认订单 |
| 用户 | 选择研究、确认模拟操作、记录外部成交 | 把模拟结果冒充正式前瞻证据 |

## 账户与证据隔离

- 用户模拟账户用于自由买入、加仓、减仓和清仓，不进入策略成绩；
- 系统影子账户用于冻结策略的自然前瞻验证，用户操作不能污染；
- 外部真实组合是只读/手工登记账本，系统不能假装已从券商取得成交；
- Historical Demo 仅用于演示产品链路，固定为 `research_only/test_only`；
- 正式 Primary 与宽样本研究各有独立协议、身份和成绩单。

## 如何判断“系统有效”

工程可运行、历史回测正收益、预测命中和真实 Alpha 是四件不同的事。要证明投资价值，需要自然
到期的 5/20 交易日样本、含成本组合曲线、可投资基准、回撤、置信区间，以及纯量化、LLM 融合
和各 Agent 的消融比较。样本不足时只能说“工程闭环已验证”或“历史研究有参考价值”。

## 文档入口

- [docs/AI_HANDOVER.md](docs/AI_HANDOVER.md)：最终验收 AI 的接管顺序、代码地图和检查清单；
- [README.md](README.md)：启动与总览；
- [docs/README.md](docs/README.md)：当前文档地图；
- [docs/DOCUMENT_STATUS.md](docs/DOCUMENT_STATUS.md)：哪些文档当前有效、哪些只作历史证据；
- [docs/handbook/02_BEGINNER_GUIDE.md](docs/handbook/02_BEGINNER_GUIDE.md)：金融与验证术语；
- [docs/HACKATHON_DEMO.md](docs/HACKATHON_DEMO.md)：隔离演示；
- [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md)：运行与部署；
- [AI_REVIEW_GUIDE.md](AI_REVIEW_GUIDE.md)：外部验收方法和声明边界。

历史 `docs/BACKEND_ROUND*.md` 只记录当时交付，不是当前能力清单。任何“当前”“最新”“已启动”
类结论必须重新从代码、数据库、任务来源和机器报告验证。
