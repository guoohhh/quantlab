# 证据与增量价值系统

> **版本化研究记录**：本文中的回放编号、样本数、分数和收益是历史证据，不是当前状态摘要。
> 当前投资价值结论必须从绑定协议与指纹的机器报告、生产数据库和自然到期样本重新形成。

## 决策闸门证据分层

`decision-gate-scorecard-latest` 将机制诊断、V1失败和V2挑战分开。V1 LLM-first 闸门已经拒绝；V2 新证据为20日4回合与5日11回合，均未达到单周期12回合门槛，且模型驱动减仓0次，所以状态固定为 `insufficient_evidence_not_promoted`。

允许声明：真实 LLM 角色输出完整；ETF veto 塌缩已修复；点时统计模型在5日盲测上显著改善概率 Brier；策略主导架构避免未经校准的文本判断清空仓位。

禁止声明：V2 已提高交易收益；LLM 能稳定择时；统计模型已带来盈利增量；历史回放能够证明未来赚钱。盈利证据报告仍为 `preliminary 60/100`，阻断项是等权基准、统计 alpha 和前瞻样本。

## 目标

系统必须分别回答三件事：

1. 确定性策略是否优于简单、低成本基准；
2. 统计概率是否优于类别频率基线；
3. LLM、多 Agent 和最终融合是否在未来到期样本中带来增量价值。

三类证据不可混用。历史回测不能证明未来 LLM 有效，单次 LLM 命中也不能证明策略盈利。

## 匿名化历史盲测

历史盲测用于回答“如果系统当时只能看到当时的数据，会如何预测和假下单”，执行契约为：

1. 在用户指定区间内自动、均匀选择非重叠日期，不读取结果后择优挑选；
2. 向 LLM 隐藏真实代码、真实日期和绝对价格，只提供匿名代码、替代日期、归一化价格路径和点时因子；
3. 每个节点重新训练统计模型，只允许 `as_of < cutoff` 且 `evaluated_at < cutoff` 的已知标签；
4. T 日收盘形成信号，T+1 真实开盘假买入，第 5/20 个交易日收盘卖出，并计入整手、佣金和滑点；
5. 同时结算纯策略、完整多 Agent 系统和沪深300同风险预算；
6. 分别报告原始 LLM、点时统计模型和最终融合概率的 Brier Score、Log Loss 与准确率。

少于10个回合只能标记为 `illustrative`，10至29个为 `preliminary`，至少30个才进入 `measured`。`evidence_qualification.qualified` 还要求规定 LLM 角色全部完成且数据链路无降级；样本等级和证据资格不能混用。超过12回合必须显式使用 `--allow-large-run` 或 API 的 `confirm_large_run=true`，避免误触发大量付费调用。即使达到30个样本，也必须结合固定区间复跑、前瞻模拟盘和统计置信区间，不能直接宣称未来盈利。

## 历史策略基准

ETF walk-forward 每个测试折同时运行：

- 训练窗选择的 ETF 轮动参数；
- 80% 沪深300 ETF 买入持有；
- 80% ETF 池等权买入持有；
- 80% 国债 ETF 买入持有。

参考组合与策略一样在 T 日生成订单、下一可用开盘成交，并使用同一 ETF 佣金和滑点模型。

策略只有同时通过以下闸门后，才解除低探索预算限制：

- 数据覆盖：所有请求标的存在，实际共同截止日距离请求截止日不超过15天；
- 绝对表现：至少3折、正收益折不低于50%、平均 Sharpe 为正、最差回撤优于 -25%；
- 相对基准：优于 ETF 等权，或收益仅小幅落后但 Sharpe 与回撤显著改善；
- 统计支持：至少504个 OOS 日、配对块自助法超额为正概率不低于95%、单侧 p 值不高于10%、27组参数试验修正后的 PSR 不低于80%；
- 过拟合：训练赢家落入 OOS 后半区的折比例不高于50%；
- 成本压力：2倍佣金与滑点下平均 Sharpe 仍为正、最差回撤优于 -30%。

正收益本身不是准入理由。参数敏感性里事后表现最好的配置只能成为下一轮研究候选，不能在看完测试折后直接激活。

## 机器证据等级

证据中心按100分自动评分：成本一致性5、可复现哈希5、数据覆盖10、防泄漏15、OOS深度15、基准15、统计支持20、成本压力10、前瞻跟踪5。

- `illustrative`：链路或样本仍不足；
- `preliminary`：已具备可复现 OOS，但一个或多个核心准入闸门失败；
- `research_grade`：历史策略准入全部通过；
- `prospective_supported`：研究级历史证据之外，前瞻预测和模拟盘也达到最低样本。

等级只约束系统可以说什么，不把历史结果转换为收益保证。JSON 与 Markdown 产物由 `quantlab evidence-report` 生成。

## 当前正式实验

validation #14 使用2015-01-01至2026-06-30真实 ETF 数据，实际共同覆盖为2015-01-05至2026-06-30；504日训练、1日隔离、126日测试，共18折和2,270个配对 OOS 日。实验 ID 为 `4600b996bfc689535cf7cd4b6190a52b6e7893403bdf022782bccf13aeac802d`，关键算法源码指纹为 `e6241664f8b2bb3909ebd4eff824b756c7adbe97d463ef7a4cac9c530695a100`。

- 策略复合收益：65.62%；平均 Sharpe：0.848；最差回撤：-19.66%；
- 相对沪深300：复合收益高17.54个百分点；
- 相对 ETF 池等权：复合收益低18.35个百分点，平均 Sharpe 低0.085；
- 2倍成本复合收益：58.63%；
- 配对块自助法超额为正概率：31.55%，单侧 p 值：0.701；
- 选参 OOS 后半区比例：33.33%。

因此绝对、数据覆盖和成本压力通过，等权基准与统计支持失败，最终为 `preliminary 60/100`，只允许低探索预算。失败结果被保存在数据库和 `data/reports/profitability-evidence-latest.*`，不会择优隐藏。

## 自适应策略候选实验

在运行结果之前，四个候选、开发期和锁定留出已经写入 `STRATEGY_RESEARCH_PROTOCOL.md`。开发期为2015-01-05至2022-12-30，锁定历史留出为2023-01-03至2026-06-30；只允许开发期冠军进入留出。

开发期冠军为 `adaptive_defensive`。锁定留出结果：

- 收益43.55%，旧轮动45.20%，沪深300 21.59%，ETF等权54.51%；
- Sharpe 1.55，旧轮动0.87，ETF等权1.34；
- 最大回撤 -5.20%，旧轮动 -13.94%，ETF等权 -9.29%；
- 2倍成本收益41.67%，Sharpe 1.49；
- 相对ETF等权收益低10.95个百分点，超额为正 bootstrap 概率11.35%。

因此候选显著改善风险效率和回撤，但未通过预注册的等权基准与统计 alpha 门槛，状态为 `historical_holdout_rejected`。它保留为保守风险画像的研究候选，`formal_strategy_changed=false`，不会替换现役策略或扩大预算。完整产物为 `data/reports/strategy-lab-latest.*`。

最新重跑 validation id 13，实验 ID 为 `1728909ecf358cca541cc52f7996538d1f19506163c88ca0da1f94620edf38fb`；V2 接入后 V1 数值与拒绝结论保持一致。

## Adaptive ETF V2.1 回顾性诊断

V2.1 增加相关性去重、50% 对角收缩协方差最大分散化、连续宽度/波动率/回撤状态倍率、35%—75% 动态核心仓位和2%再平衡容差。2023-01-03至2026-06-30复用历史区间的探索结果为：收益44.66%、Sharpe 1.53、最大回撤-5.41%，2倍成本收益43.09%；ETF等权收益54.51%、Sharpe 1.34、最大回撤-9.29%。V2 风险效率更高但收益落后9.85个百分点，因此状态固定为 `retrospective_exploratory_only`，不能形成准入或 alpha 声明。

## 前瞻概率消融

每条线上预测分别冻结：

- `raw_llm`：原始 LLM 概率；
- `statistical`：时间外准入统计模型概率；
- `final_ensemble`：最终融合概率。

预测到期后，证据中心分别计算 Brier Score、Log Loss、准确率、平均置信度和校准误差。少于配置的最低到期样本时，状态只能是 `collecting`，不得宣称增量有效。

## 前瞻组合消融

模拟盘从首次运行当天开始，同时保存四条不可回写曲线：沪深300、ETF 等权、纯轮动和完整系统。纯轮动与完整系统的差异用于观察多 Agent 风控带来的未来收益、回撤和换手变化。
