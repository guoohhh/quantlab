# QuantLab 第一版 Demo 指南

本指南用于黑客松现场演示。完整链路使用真实市场数据和当前配置的 GPT/DeepSeek 路由；系统只生成研究和手工下单建议，不连接券商。

## 演示前检查

```powershell
cd E:\LLM-Projects\quant-lab\my-system
.\.venv\Scripts\Activate.ps1
quantlab doctor
quantlab market-radar
streamlit run dashboard/app.py
```

验收重点：

- `doctor` 不显示任何 API Key。
- 市场雷达的 `coverage.available` 应大于 0，数据失败必须出现在 `degraded_sources`。
- 前端侧栏可选择速度、平衡、质量预设，也可逐 Agent 选择模型和推理强度。

## 建议的 5 分钟现场流程

1. 打开“今日决策”，展示市场状态、建议总暴露、最近决策与下一步事项。
2. 打开“市场雷达”，解释 20/60/120 日强弱、波动率、市场宽度和风险偏好都可复算。
3. 打开“股票发现”：搜索股票名称，展示用户自选池快速筛选；再运行五风格系统推荐，解释它不调用 LLM，只生成研究优先级。
4. 在“批量深度会诊”选择1只股票，展示 Buffett、Munger、Graham、Fisher、Lynch 与战术委员会如何分工。现场若选择多只，先说明每只约18次角色调用。
5. 打开“候选擂台”，用2个候选演示同一委员会横向选优、Reviewer过滤、正相关去重和组合压力测试。明确预计会产生22次角色调用。
6. 打开“证据中心”，主动展示 ETF 轮动当前未通过等权基准闸门，因此资金预算已受限。
7. 打开“模拟盘”，展示四个从首次运行日起不可回写的影子账户和次日开盘成交契约。
8. 打开“历史盲测”，加载已保存的 Replay #3：先展示匿名化、点时训练与 T+1 开盘成交契约，再揭示真实结果。
9. 打开“一键 Demo”，运行真实数据链路。系统会选择当前雷达中的领先 ETF，而不是使用写死标的。
10. 展示 Agent 委员会职责、5/20 日概率、统计模型融合权重、Reviewer 和确定性决策轨迹。
11. 下载 Markdown 报告与 JSON 审计包，强调报告不包含 Key 和完整敏感 Prompt。
12. 打开“手工下单计划”，说明相对基准准入、动态预算、整手约束和人工执行边界。

候选擂台中的30%等权组合是统一口径的比较组合，不是建议仓位或订单。历史风险只对有对齐行情的标的计算，缺失标的会在 `missing_symbols` 中公开；四种情景冲击是可手算复现的确定性假设，不冒充预测。

如果数据库中已有超过5/20个交易日的历史擂台，点击“结算已到期擂台”，展示 Agent 第一名相对原雷达第一名和全候选等权的真实超额，以及排名 IC、第一名胜率和事后遗憾。样本不足30时界面必须显示 `illustrative`，不能把一两次胜出包装成有效策略。

## A 股投资大师演示

在“多 Agent 研究”选择“A股”，输入例如 `sh600519`，保留“采集近期免费新闻与公告”。股票路径会增加：

- 财务质量确定性闸门；
- Buffett、Munger、Graham、Fisher、Lynch 独立职责；
- 新闻和公告事件；
- 估值/风险否决与最终 Reviewer。

免费数据端点可能变动或限流。任何失败都会降低数据质量并显示在报告中，不会用虚构内容补齐。

## 无网络回退

“一键 Demo”页保留离线合成数据回退。它只证明软件链路可运行，界面会明确标注 `synthetic demo data`，不得将其收益或决策作为真实市场结果展示。

## 当前真实验收样本

2026-07-13 的验收中：

- 市场雷达覆盖 6/6 个 ETF，无降级；
- 自动选择 `sh513100` 进入研究；
- 完成 11 次 LLM 调用；
- Reviewer 状态为 `approved`；
- 最终动作为 `watch`，说明系统不会为了演示强制给出买入；
- 审计报告已生成在 `data/reports/latest-demo.md` 和 `data/reports/latest-demo.json`。

同日固定区间历史盲测 Replay #3：

- 自动抽取 2025-01-02 与 2025-06-20，预测时隐藏真实 ETF、真实日期和绝对价格；
- 向 Agent 提供截至当日的120日归一化价格路径，不泄露未来数据；
- 22/22 个规定角色输出成功；Bear 角色首选端点超时一次后成功回退，角色清单仍完整；
- 三条比较曲线分别连续复利，纯策略累计 `-0.3317%`；完整系统分别给出 `review_required` 和 `watch`，均未批准买入，累计 `0.0000%`；沪深300同风险预算 `+1.3095%`；
- 报告位于 `data/reports/historical-replay-3.md` 和 `data/reports/historical-replay-3.json`。

这两个回合只能证明端到端工程链路和当时的风险过滤效果，证据等级为 `illustrative`，不代表未来投资结论。
