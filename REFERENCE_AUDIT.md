# 参考项目审计与吸收清单

> **历史参考审计（基线 2026-07-14）**：许可证与来源记录仍有审计价值，但“当前状态/下一步”必须重新核对本地快照、上游仓库和当前 QuantLab 实现。

审计基线日期：2026-07-14。新增参考项目通过 GitHub 官方 zipball 下载到 `../third-party/`；
目录不含 `.git` 历史，因此本文件记录仓库身份和许可证，但不能把本地快照当成固定 commit。

本轮审计不只阅读 README，还核对了关键源码、测试、任务状态、错误降级和许可证。判断标准是：

1. 文档宣称的能力是否有真实代码和测试；
2. 是否适合 QuantLab 的“研究辅助 + 手工下单”边界；
3. 是否允许未来黑客松展示和商业化；
4. 是否能提高投资研究质量，而不只是增加页面数量。

## 新增参考项目

| 项目 | 仓库/许可证 | 真实强项 | 限制与决策 |
|---|---|---|---|
| QuantDinger | `brokermr810/QuantDinger`，Apache-2.0 | 自托管 AI Trading OS；Agent Gateway；R/W/B/N/C/T 权限域；Token 市场/标的白名单；幂等任务；SSE 进度；审计；策略信号执行契约；策略开发/回测/运行时分层 | Agent job 仍以进程内线程池为主；部分生产并发问题在文档中仍列为待补；其自动实盘范围超出 QuantLab 第一版边界 |
| khquant-skill | `khscience/khquant-skill`，MIT | 自然语言意图路由；版本预检；`doctor` 环境诊断；查询/修改/危险三色安全分级；命令回显；按场景加载最小参考包 | 它是外部 `kh` CLI 的操作知识包，不是回测内核；适合参考未来 QuantLab Skill，不直接成为系统运行依赖 |
| OSkhQuant | `khscience/OSkhQuant`，CC BY-NC 4.0 | A 股本地回测工作台；`init/khHandlebar/khPreMarket/khPostMarket` 回调；MyTT 指标；MiniQMT；成本、滑点、T+1 可卖数量；桌面数据管理和回测结果窗口 | 非商业许可证与本项目未来商业化冲突，禁止复制代码；源码高度单体化；模拟/连接路径存在占位；只能学习接口和产品设计 |
| daily_stock_analysis | `ZhuLinsen/daily_stock_analysis`，MIT | 多市场数据 fallback；后台任务队列；SSE；运行拓扑；版本化 AnalysisContextPack；字段级数据状态；Agent 问股与多轮会话；分歧摘要；风险下调；信号前瞻结算；风险画像；通知路由和降噪；Web/Electron/GitHub Actions 产品闭环 | 任务队列主要驻留进程内；Agent 主链多为顺序执行；数据质量权重与 hit/miss 规则仍是启发式；借鉴契约与产品闭环，不照搬评分结论 |

### QuantDinger 最值得吸收的能力

- Agent 与人类 API 分离，机器 Token 使用独立权限域、限流、审计和撤销生命周期。
- 长任务统一采用 `submit -> job_id -> poll/SSE -> terminal result`，并保存幂等键。
- 策略明确声明 `signal_form`、`exit_owner`、`flip_mode`、确认 K 线和下一根开盘成交契约。
- 写操作和外部副作用必须幂等或串行化；worker 必须先 claim 再调用外部系统。
- MCP 工具不是把所有内部函数暴露出去，而是窄化、审计、分级的产品表面。

不直接采用：自动实盘、交易所凭据管理、支付/会员系统，以及未经独立时间外验证的自动阈值校准。

### khQuant 最值得吸收的能力

- 把“策略代码”和“回测配置”分开：策略只描述逻辑，资金、成本、滑点、周期和风险由配置控制。
- 回调接口简单、可学习，适合未来 QuantLab 策略实验室的用户扩展层。
- `khHistory(current_time=...)` 明确防未来数据；T+0/T+1 按标的类型区分。
- 回测结果中心包含净值、回撤、月度热力图、交易记录、滚动指标和多次回测对比。
- Skill 的安全分级、环境诊断、版本能力矩阵和渐进式 references 组织方式值得直接学习。

不直接采用：OSkhQuant 源码、单体 PyQt 架构、自动实盘占位路径、固定的结果好坏阈值。

### daily_stock_analysis 最值得吸收的能力

- `AnalysisContextPack` 把 quote、K 线、技术、筹码、基本面、新闻、持仓分别标成
  `available/fallback/stale/partial/missing/fetch_failed`，并形成低敏数据质量摘要。
- Run Flow 将数据源重试、fallback、上下文组装、LLM、报告保存和通知画成可追踪拓扑，并识别瓶颈。
- 前瞻结果按 1/3/5/10 日交易窗口结算，保留 unable 原因、重试语义、分市场/动作/质量统计。
- 多 Agent 先生成低敏分歧摘要，再由决策 Agent 综合；风险角色只能 veto 或向保守方向下调。
- 用户可在不重新抓数据、不重新调用全部 Agent 的情况下，用不同决策 profile 预览同一报告。
- 通知具有路由、严重级别、静默时段、去重、冷却和发送中预留，产品完成度高。

本轮已落地：基于冻结研究报告的“专家圆桌”，支持自选大师、多轮交锋、主持人综合、逐轮审计和持久化；
它只属于探索研究层，不修改正式决策、仓位或订单。

## 既有参考项目复核

| 来源 | 经源码复核后保留的价值 | QuantLab 采用方式 |
|---|---|---|
| TradingAgents | 显式 debate state；Bull/Bear 轮流回应；激进/中性/保守风险辩论；Manager 最终裁决；历史决策记忆 | 保留结构化状态和对抗研究；不让自由文本裁决绕过确定性风控 |
| AI Berkshire | 信息丰富度偏见检查；双源财务验证；精确十进制计算；报告随机抽检；四大师第一性原理追问 | 已吸收证据治理和大师角色；圆桌进一步支持用户自选人物 |
| ai-hedge-fund | 多投资人风格的确定性子评分 + LLM解释；分析师并行；风险经理给 allowed actions | 学习“确定性先算、LLM后解释”；不采用未经 A 股 PIT 验证的固定分数和阈值 |
| Qlib | PIT 数据、Dataset/Model/Strategy/Executor/Account 分层、实验记录器、在线/离线模型治理 | 已用于学习系统、回测和账户分层的设计基准 |
| FinGPT | 周期化组织公司介绍、历史价格、新闻、基本面和市场情绪；LoRA 训练；预测解释 | 已吸收预测上下文与事后学习；不把未来标签或预测方向泄漏进推理输入 |
| westock | A 股自然语言研究工具和多命令数据表面 | 仅通过隔离适配器使用；Markdown 输出不作为唯一结构化契约 |
| IronQ | 动量加速度、路径质量、量价非对称、市场状态适配、投资大师委员会 | 已完成主要因子和委员会能力；权重仍需滚动 IC/样本外验证 |
| AlphaForge | LangGraph、多 Agent 状态、审计日志、意图路由、透明降级 | 已用于编排和审计；简单信号计数不能替代真实回测 |
| SmartInvest | 代码归一化、缓存 TTL、多源 fallback、数据层与智能层解耦 | 已用于行情适配和显式降级 |
| LUCID | 证据覆盖、一致性、认知偏差惩罚、主动证伪 | 已进入 Reviewer、Bull/Bear 和证据中心 |
| CorTex | 结构化决策卡、多空冲突、事后归因 | 已进入决策卡、人工复核和学习闭环 |
| ETF Pilot | 分数凯利、波动率上限、现金作为仓位 | 已进入动态预算和组合规划；凯利比例受准入门槛限制 |
| RiskRadar | 财务红旗、偿债能力、硬风控否决 | 已进入财务质量与风险闸门；不使用跨行业统一 Z-Score |

## 可直接吸收、只学思想、明确不采用

### 可直接吸收

- MIT/Apache 项目的接口契约、任务状态、上下文质量状态、运行拓扑、通知治理和测试方法。
- 用户自选专家的圆桌、同轮并行、跨轮回应、主持人综合和探索层权限边界。
- Agent/Skill 的查询、修改、危险三级授权模型。
- 策略实验室未来使用明确的信号/退出/成交契约与认证等级。

### 只学思想

- OSkhQuant 的回调、GUI 工作流、MyTT 使用体验和本地数据管理；受 CC BY-NC 限制，不复制实现。
- QuantDinger 的实盘权限和交易运行时；第一版只借鉴权限与审计，不开放自动下单。
- FinGPT 的开源模型训练路径；需要先完成自己的无泄漏数据集和基线，不能直接训练后宣称有效。

### 明确不采用

- 未经独立样本外验证的固定 Agent 权重、命中率或收益宣传。
- 把“角色名称 + 长 Prompt”当成已完成的金融能力。
- 让 LLM 计算手续费、技术指标、点时可见性、仓位上限或可卖数量。
- 数据失败时静默替换成示例数据。
- 让圆桌、Chat 或 MCP 绕过 Reviewer 和确定性风险闸门。
- 复制 CC BY-NC 源码进入可能商业化的 QuantLab。

## 许可证边界

| 许可证 | 处理原则 |
|---|---|
| MIT / Apache-2.0 | 可在保留许可证和归属的前提下参考或复用；仍优先独立实现以保持架构一致 |
| CC BY-NC 4.0 | 只做只读研究和设计借鉴；不复制到 `my-system`，不形成衍生实现 |
| 无明确许可证 | 默认不可复制代码，只能学习公开产品思想 |

详细实施优先级和验收条件见 `REFERENCE_INTEGRATION_ROADMAP.md`。
