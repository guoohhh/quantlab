# 持续学习系统

> **版本化研究记录**：本文保存学习系统的设计和历史实验。样本数、模型状态、指标与训练截止日
> 不代表当前数据库；验收时需核对目标数据库、协议、源码/数据指纹和自然到期记录。

## A股点时训练准入（2026-07-14）

固定当前股票池的历史样本继续禁训。新增的 `stock_market_point_in_time` 样本来自指定历史日真实市场、同日交易/ST状态和确定性分层抽样；只有所有截面均来自 BaoStock、存在交易所主数据版本且每个截面 Jaccard 均不低于0.90时，`training_eligible` 才为真。

30回合真实市场回放累计形成1,136条可训练5日点时样本。最新挑战使用1,101条训练样本和511条滚动验证观测；候选 Brier 为0.2056（基线0.2044），Log Loss为1.0160（基线1.0042），三折通过率0%，因此拒绝激活。20日点时市场样本仍不足，固定池偏差样本不会被用来凑满门槛。

这说明“能够训练”和“训练后有用”是两件事。系统保留被拒模型、验证指标和挑战原因，不允许用强制参数绕过第一只股票模型的基线准入。

## 2026-07-14 扩展训练与盲测结果

ETF 历史学习库已从 2023 起点扩展到 2015-01-01，5日和20日各3,322条机械生成、可审计样本。新候选使用2,913/2,892条训练样本和1,622条验证样本：5日 Brier 0.1903 对基线0.2132，20日0.1902对基线0.2217，三折通过率均100%。由于挑战留出不晚于现役模型训练截止日，两者保持 `challenge_pending`，未自动激活。

Replay #9 的11个5日回合提供了独立概率消融：统计模型 Brier 0.1713、准确率63.6%；原始LLM为0.2257和27.3%；融合为0.1960和45.5%。这证明点时学习改善了该短样本的概率质量，但 `calibrated_strategy_primary_v1` 的冻结看空阈值未触发，因此没有交易收益增量证据。

QuantLab 的“学习”不是让 LLM 在后台不可控地改写自己，而是由可审计的四层闭环组成。

## 1. 决策时特征冻结

每次 5/20 日预测会冻结当时可见的信息：

- 9 个量化因子、日周月共识、回撤反转强度
- 市场状态、策略分数、数据质量
- 战术委员会和长期委员会分数、否决状态
- 财务质量及关键财务指标
- Quant/Fundamental/News/Bull/Bear Agent 分数
- 混合前的原始 LLM 涨/平/跌概率

未来新闻、财报和实际价格不能回写到预测日特征。

## 2. 历史与在线样本

历史样本按时间逐点生成：在日期 T 只使用 T 及以前的行情计算因子，标签使用 T+5 或 T+20 的实际收益。线上预测到期后进入同一学习样本库。

每条样本同时保存：

- `as_of`：预测发生时间
- `evaluated_at`：真实结果变得可知的时间
- `asset_scope`：ETF、股票或可转债
- 特征、真实收益、涨/平/跌标签、来源和上下文

训练切分不仅要求 `as_of` 早于验证期，还要求 `evaluated_at` 早于验证期，避免结果尚未揭晓的样本泄漏。

## 3. 模型训练与准入

当前统计模型为带标准化、L2 正则、类别不平衡权重和嵌套时间概率校准的多分类 Softmax 模型。选择它是因为：

- 输出天然是涨/平/跌概率；
- 参数和特征系数可以完整序列化、审计和回滚；
- 少量样本下比深度网络更不容易产生虚假精度；
- 不依赖外部黑盒训练服务。

ETF 历史样本同时包含点时横截面特征：20/60/120 日动量排名、20 日波动率排名、相对收益、市场宽度、横截面离散度和领先差。所有排名只使用 `as_of` 当日及以前的轮动池数据，实时预测使用同一套计算器，避免训练/线上特征偏移。

每个外层滚动训练窗内部会再留出最后一段已揭晓样本，选择向训练期类别先验收缩的概率幅度。校准参数只能看到外层训练期，不能看到外层验证期；它用于抑制少数过度自信的错误预测，主要改善 Log Loss。

验证采用最多 3 个扩展训练窗 + 后续验证窗的滚动时间折。每一折训练数据不仅要求 `as_of` 早于验证窗，还要求标签的 `evaluated_at` 已经早于验证窗。候选模型必须同时满足：

- Brier Score 优于训练期类别频率基线；
- Log Loss 优于类别频率基线；
- 至少 67% 的滚动验证折同时改善 Brier Score 与 Log Loss；
- 达到最低训练和验证样本数。

否则模型会登记为 `rejected`，不会激活。每个模型版本保存验证指标、训练截止时间和涨/平/跌最重要特征系数。

通过类别基线只是候选资格，不再足以替换线上模型。已有冠军时，候选必须在冠军 `trained_until` 之后开始的时间留出段同场比较，并同时满足：候选 Brier 更低、Log Loss 不高于冠军、配对 bootstrap 判断候选 Brier 更优的概率至少90%。结果记录为 `promoted`、`rejected` 或 `challenge_pending`。没有前瞻留出时禁止替换。

旧版本若没有冠军—挑战者晋级记录，会显示 `legacy_awaiting_challenge`。当前生产政策已将这类 legacy 统计模型的集成权重强制降为0，直到积累足够的新到期样本并完成真正前瞻挑战。

模型按 `asset_scope + horizon_days` 隔离。ETF 模型不能用于股票或可转债。

## 4. LLM 与统计模型集成

激活模型只作为概率集成的一部分。统计权重由样本外 Brier 改善幅度和验证样本量决定，上限 50%。系统保存：

- 原始 LLM 概率
- 统计模型 ID、版本和概率
- 实际集成权重
- 最终概率

因此可以分别评估 LLM、统计模型和最终集成，而不是把结果混在一起。

## 5. 结果回填与事件归因

预测到期时，系统获取真实行情并计算收益，同时采集预测期间的新闻事件。事件按发布时间、声明影响度和情绪方向与预测误差的匹配程度排序。

归因结果只能称为候选解释。系统会计算实际类别概率、单样本 Brier、置信度与误差严重度，并把错误归入可统计的诊断候选：模型过度自信、LLM/统计模型分歧、数据源降级、财报/宏观/监管/新闻事件或未解释残差。

归因边界：

- `causal_claim` 永远为 `false`
- 没有事件证据时标记 `unresolved`
- 免费新闻接口覆盖有限时明确展示 coverage warning

事件关联仍不是因果证明。后续可使用事件前后异常收益和对照组进一步提高归因质量。

日常运行使用 `quantlab learning-cycle`：它会结算到期预测、采集期间事件、生成归因，并只在出现新完成样本时重新训练对应资产域模型。没有新样本时不会制造重复模型版本。

## 模型漂移

当某个模型积累至少 30 条线上到期样本后，系统分别计算统计模型、原始 LLM 和最终集成的近期 Brier Score。如果统计模型超过历史验证分数的 1.25 倍，或劣于类别频率基线，系统会自动停用该版本并记录原因。停用不删除模型，仍可审计和回滚。

## 股票样本治理

ETF 模型只服务配置中的固定 ETF 轮动池。用户手工提供的当前股票列表存在幸存者偏差和选择偏差，因此可以生成研究样本，但 `training_eligible=false`。股票模型只读取全市场点时分层回放中逐截面完整性通过的样本；当前5日候选已经训练但未击败类别先验，因而没有线上股票模型被激活。

## 当前真实滚动验证结果

数据范围：2023-01-01 至 2026-07-10，6 个 ETF，每 5 个交易日取一个预测截面。

| 周期 | 样本 | 三折验证样本 | 模型 Brier | 基线 Brier | 模型 Log Loss | 基线 Log Loss | Fold pass rate | 状态 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| 5 日 | 996 | 594 | 0.1921 | 0.2098 | 0.9769 | 1.0454 | 100% | version 7，等待前瞻冠军挑战 |
| 20 日 | 996 | 594 | 0.2047 | 0.2197 | 1.0224 | 1.0875 | 100% | version 7，等待前瞻冠军挑战 |

旧版单次尾部切分模型和缺少横截面特征的三折候选均未通过治理。加入点时相对排名与嵌套概率校准后，5 日和 20 日模型在三个外层验证折中都同时改善 Brier Score 与 Log Loss。version 7 是升级冠军—挑战者治理前的现役版本，因此现在被明确标记为 `legacy_awaiting_challenge`；version 8 候选因留出期早于冠军训练截止日而记录为 `challenge_pending`，没有替换冠军。

最近的历史验证若只按 Brier 改善计算，5 日和20日候选曾分别得到约42.3%和34.1%的理论权重；但这些候选没有完成现役冠军训练截止日之后的前瞻挑战，legacy 冠军也没有合格晋级记录。因此当前真实 ETF 决策中的统计模型生产融合权重为0。后续必须积累新的到期样本、完成冠军—挑战者比较并持续监测漂移，不能因为历史时间折通过就视为永久有效。
