# QuantLab 第三轮策略预注册协议

协议版本：`round3-pit-forward-v1`  
冻结日期：`2026-07-17`  
适用范围：点时 ETF 池、A股 V4、可转债证据、前瞻消融与动态预算。

## 不可越过的证据边界

1. 任何历史回放、参数开发、修复后重跑均标记为 `research_replay`，不得写入前瞻到期样本表。
2. 只有在本协议冻结后实时登记、当时未见结果、达到真实到期日后结算的预测，才计入 `forward_shadow`。
3. 每个变体和期限至少有 30 个真实到期样本，且数据与角色完整性达到协议阈值，才可进入 `measured`。
4. 2023—2025 已被开发过程查看，不再作为独立验证。2026-01-01 至本协议冻结日同样只可作为研究回放；冻结日之后的数据按自然时间持续积累。
5. 失败、无效、数据缺失和未触发结果全部保留。协议、阈值或策略发生变化时必须创建新版本，不能覆盖旧版本。

## 点时 ETF 池协议

- 每个历史时点仅允许使用 `listing_date <= as_of`、尚未退市、当日可交易且其状态在截止时间前已可获得的 ETF。
- 证券主数据、历史交易状态、成交额、规模、流动性、资产类别、海外溢价和交易时差均记录来源、`available_at`、版本和指纹。
- 同类代表按当时可获得的规模与流动性排序确定，稳定同分时按证券代码排序；代表允许随时间切换。
- 缺少可靠规模、溢价或跨市场时差信息时明确标记 degraded/unavailable，不补造。
- 今天配置中的六只 ETF 仅是当前产品默认项，不是历史固定可投资池。

## 前瞻消融协议

冻结变体：

1. `simple_baseline`
2. `quant_only`
3. `raw_llm`
4. `statistical_model`
5. `llm_stat_fusion`
6. `llm_trade_gate`
7. `full_system`

固定期限为 5 日和 20 日，并同时记录组合账户结果。每个变体记录方向概率、动作、目标仓位、是否实际触发、数据完整性和角色完整性。结算指标为到期样本数、Brier、Log Loss、准确率、成本后收益、最大回撤、换手、相对纯量化增量和相对简单基准增量。

前瞻样本登记必须满足：`prediction_as_of >= cohort_frozen_at.date()`，且登记时不得带有实际结果。结算必须发生在 `due_at` 之后，并保存观测时间与结果来源。

## A股 V4 协议

- 候选池使用点时证券主数据和点时每日状态；历史 ST、停牌、退市、代码变化按当时状态处理。
- 每日候选应覆盖可获得的全市场或接近全市场范围，并保存母池数量、过滤原因和数据缺口。
- 组合采用分散 Top-K；固定执行行业、市值暴露与相关性约束。
- 风险关闭时持有现金或协议指定的防御资产，不强制持有风险资产。
- 比较同暴露沪深300，报告成本后收益、Rank IC、回撤、换手和区块 Bootstrap。
- V4 的所有历史运行只属于开发/研究证据。失败结果以追加记录保存，不允许反复调门槛后覆盖失败。

初始固定政策：Top-K=8；单行业最多 25%；单一市值桶最多 40%；入选对之间 60 日相关性不得高于 0.85；风险开启总暴露上限 60%；风险关闭风险资产上限 0%，现金为默认防御资产；最小平均成交额 5,000 万元；交易成本使用系统 A股成本模型。

## 可转债协议

- 点时债券池必须记录上市、到期、退市、停牌、历史余额、强赎状态、评级及变化、流动性和来源可用时间。
- 使用 Walk-forward，并与简单双低基准比较；生产预算在证据准入前固定为 0。
- 缺少强赎、余额、评级或真实流动性字段的债券不得被描述为生产可投，只能进入降级研究池。
- 前瞻影子账户遵守与消融系统相同的真实登记和到期结算规则。

## 动态预算协议

- 预算变化采用跨期指数平滑，并限制单日目标权重变化。
- 低于最小调仓阈值的漂移不交易；交易量按整手向下取整。
- 先保留现金储备，再分配风险预算；换手成本和冲击成本进入计划。
- 模型或证据单日变化不能直接造成大幅换仓。证据降级可以立即降低上限，但恢复风险预算必须渐进进行。

## 变更治理

协议内容由实现计算 SHA-256 指纹。任何阈值、变体、样本门槛或证据定义变化都必须产生新协议版本、新 cohort 和新指纹。旧记录保持只读并继续可审计。
