# ETF 决策闸门反事实审查协议

登记日期：2026-07-14。本协议在新的独立历史盲测运行前冻结。

## 问题定义

Replay #2–#4 已经被查看，只能作为诊断集。它们显示：纯 ETF 轮动策略既有盈利回合也有亏损回合，而当前完整系统因为统一的高确信买入阈值几乎不参与。Replay #4 的概率预测方向正确，但 4 个盈利回合全部被映射为零仓位；Replay #2–#3 中零仓位又避开了小额亏损。

因此不能简单放宽闸门。需要回答的是：在保留 Reviewer、否决权、高冲突复核和证据完整性约束后，小额、分层的 ETF 战术仓位是否能比“全部不交易”和“纯策略全额参与”取得更好的风险收益折中。

## 冻结比较臂

1. `current_strict`：保持当前正式规则。只有最终动作是 `buy/add`、Reviewer 批准且不需要人工复核时才使用确定性决策卡仓位。
2. `etf_score_tiered_v1`：保留全部安全阻断条件。综合分不低于 0.30、预测上涨减下跌概率不低于 0.10、预测收益为正且预测置信度不低于 0.45时，使用纯策略仓位的 75%；综合分不低于 0.12、概率差不低于 0.20、上涨概率不低于 0.50、预测收益为正且预测置信度不低于 0.55时，使用纯策略仓位的 35%。
3. `etf_forecast_confirmed_v1`：保留全部安全阻断条件。综合分不低于 0.12、策略分不低于 0.20、上涨概率不低于 0.50、上涨减下跌概率不低于 0.20、预测收益为正且预测置信度不低于 0.55时，使用纯策略仓位的 50%。
4. `strategy_unless_bearish_v1`：解释性消融，不具备直接晋级资格。保留全部安全阻断条件；只要预测收益非负且上涨减下跌概率高于 -0.10，就使用纯策略全部仓位。它用于判断 LLM 的主要价值究竟是确认上涨还是只否决明显看空回合。
5. `strategy_only`：不经过 Agent 闸门的纯策略同风险预算账户。

“全部安全阻断条件”固定为：Reviewer 必须批准、Council 不得触发否决、不得触发高冲突复核、确定性决策置信度不得低于 0.25、策略方向分必须为正。任何一个条件失败时仓位为零。

## 数据与执行契约

- 新的确认性运行不得使用 Replay #2–#4 的相同回合。
- 优先使用 2015–2023 年的匿名化、非重叠回合；2024–2025 年保留为已看过的诊断区间。
- 预测周期分别报告 20 日与 5 日，不得把两个周期混成独立同分布样本。
- 每个比较臂使用独立、路径依赖的资金曲线；T 日收盘生成信号，T+1 日开盘买入，周期末收盘卖出，整手、滑点和 ETF 费用保持一致。
- 盲测归一化价格 100 仅是指数，不得写入可执行入场价字段。
- 所有候选阈值在新运行完成前不得修改。若失败，保留失败报告，不根据结果继续调到通过。

## 固定报告指标

- 累计收益、平均回合收益、参与率、交易胜率、最大回撤和期末权益。
- 相对 `current_strict` 与 `strategy_only` 的累计收益差。
- 避免亏损、错过盈利、减少亏损、削减盈利和完整参与的回合数。
- 最差回合、最大单回合贡献、留一法最低累计收益，用于识别单样本驱动。
- 最终概率、原始 LLM 概率和点时统计模型的 Brier Score 与方向准确率。
- LLM 角色调用完整性、数据源降级和样本资格。

## 晋级规则

候选只能晋级到独立前瞻影子账户，不能直接替换正式策略。至少满足：

1. 新的未见回合不少于 12 个；30 个以上才可称为测量级历史证据。
2. 相对 `current_strict` 的净收益为正，且不是由单个回合贡献全部改善。
3. 最大回撤不差于 `strategy_only`，最差回合损失不因闸门放大。
4. 参与率至少 20%，避免通过持续空仓获得虚假的低回撤。
5. 在至少两个不重叠日历子区间中方向一致；若 5 日与 20 日结论相反，分别保留，不做合并宣传。
6. Reviewer、否决权、高冲突复核或数据完整性约束没有被绕过。

即使全部通过，也只登记为 `prospective_shadow_candidate`；最终是否提升真实决策能力需要依赖冻结后的前瞻模拟盘。
