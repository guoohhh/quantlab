# A股横截面策略预注册协议 V1

登记时间：2026-07-14。在2018—2025扩展点时数据首次运行候选之前冻结。

## 研究目标

解决 Replay #7 暴露的两个问题：透明先验排名的平均 Rank IC 为负；上涨/横盘/下跌分类模型没有改善横截面选股。新研究直接优化“同一历史截面内谁的未来5日相对收益更高”，不把分类准确率冒充选股能力。

## 数据分区

- 开发期：2018-01-01 至 2022-12-31；
- 验证期：2023-01-01 至 2025-12-31；
- 锁定留出：2026-01-01 至 2026-07-13；
- 前瞻期：协议登记后的影子盘，不可回写。

开发期可比较全部预注册候选并选择唯一冠军。验证期只运行开发期冠军；若失败，可以在不打开2026留出的前提下登记V2，但V1不能改参数。2026留出只允许打开一次，打开后任何修改都只能进入新的前瞻协议。

## 共同数据与执行

- 每个信号日使用真实点时A股股票池；
- 按沪主板、深主板、科创板、创业板确定性分层抽样；
- 排除同日ST、停牌、上市历史不足和20日平均成交额不足5000万元的股票；
- 特征只使用T日收盘前数据；标签为未来5个交易日后复权收益；
- 开发筛选使用每次交易0.35%的保守往返成本；
- 最终验证和锁定留出必须使用T+1开盘、100股整手、最低佣金、印花税、过户费、滑点、停牌和一字涨跌停的完整执行器；
- 主动仓位固定为15%，其余持有现金；基准为同15%暴露沪深300指数。

## 固定候选

所有静态候选先把下列横截面特征映射到0—1或-1—1，再按固定权重求和：

1. `reversal_low_vol`：20日反转45%、低波25%、120日趋势20%、路径质量10%；
2. `quality_momentum`：60日动量30%、120日动量25%、路径质量20%、低波15%、量价非对称10%；
3. `momentum_reversal_blend`：60日动量30%、120日动量20%、20日反转25%、低波15%、路径质量10%；
4. `low_vol_trend`：低波35%、120日动量25%、路径质量20%、价格位置10%、因子综合10%；
5. `trend_pullback`：120日动量30%、60日动量25%、20日反转20%、回撤修复15%、低波10%；
6. `ridge_rank_v1`：使用相同点时特征预测截面百分位归一化后的连续未来收益排名；L2系数固定为10，不搜索参数。开发期成绩使用至少12个既有截面后的扩展窗点时预测，不能用开发期同日标签给自己打分；验证期只使用完整开发期拟合一次并冻结的参数。

现有 `screen_score` 作为公开基线，不参与候选数量计算。

## 开发期选择

每个候选按信号日选择第一名，计算：

- 扣成本累计收益、最大回撤和胜率；
- 相对同暴露沪深300的平均超额及配对块Bootstrap；
- 横截面Spearman Rank IC；
- 年度正收益比例和年度正超额比例。

候选必须先满足开发期累计收益为正、平均超额为正、Rank IC为正、至少60%的年度收益为正。合格候选对平均超额、Rank IC、最大回撤和正超额年度比例分别做候选内百分位排名，再按35%/30%/20%/15%合成选择分；并列时选择权重更简单的静态公式。

## 验证期门槛

开发期冠军必须同时满足：

- 验证期累计收益为正；
- 相对同15%暴露沪深300平均超额为正；
- 配对Bootstrap超额为正概率不低于90%，90%区间下界不低于-0.10%/回合；
- 平均Rank IC为正；
- 最大回撤不超过10%；
- 至少2个验证年度收益为正，且至少2个年度跑赢基准。

验证失败不得打开2026锁定留出。

## 锁定留出准入

用2018—2025全部数据重新拟合一次开发期冠军（静态公式不变，Ridge只更新系数），然后冻结。2026留出必须同时满足：

- 扣完整交易成本后累计收益为正；
- 同暴露沪深300超额为正；
- 配对Bootstrap超额为正概率不低于90%；
- 最大回撤不超过8%；
- 参与率不低于70%。

通过后也只进入独立前瞻影子账户；至少30个新的前瞻结算样本后，才允许申请成为默认股票推荐排序。
