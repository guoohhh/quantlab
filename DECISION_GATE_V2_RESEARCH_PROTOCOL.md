# ETF 决策闸门 V2 研究协议

登记日期：2026-07-14。本协议在 V2 候选首次运行前冻结。

## 已知诊断与失败保留

Replay #7 是 2015–2017 年的 12 个未见、非重叠、20 日回合。纯策略累计收益为 +4.60%，现行完整系统为 -1.56%，`etf_score_tiered_v1` 为 -1.82%，`etf_forecast_confirmed_v1` 为 -0.94%，`strategy_unless_bearish_v1` 为 +0.17%。V1 候选全部未通过，不得删除或用后续结果覆盖。

盈利回合的平均 LLM 上涨减下跌概率差为 0.277、平均综合分为 0.252；亏损回合反而为 0.314 和 0.364。未经校准的 LLM 与统一综合分没有表现出 ETF 20 日择时区分力。Reviewer 还曾在确定性规则未触发时主观扩展高冲突条件，错误阻断大幅盈利回合。

学习库原有样本从 2023 年开始，早期 Replay 无法按点时约束使用统计模型。现已从本地缓存机械生成 2015-01-01 至 2026-06-30 的历史因子样本，5 日和 20 日各 3,322 条。新训练的 20 日模型在 1,622 个验证样本上 Brier 为 0.1902，优于滚动基线 0.2217，三折通过率为 100%；但因缺少更新的前瞻挑战期，仍保持 `challenge_pending`，不得自动替换现役模型。

## 第一性原理分工

- ETF 的可重复交易优势首先来自量化策略、成本模型和风险预算，不来自 LLM 文本判断。
- LLM 负责解释、情景、反证和概率表达；没有校准证据时不拥有 ETF 择时权。
- 只有点时训练、验证通过并被预测器实际接纳的统计模型可以缩减策略仓位。
- LLM Reviewer 是审计证据，不得凭未写入规则的主观条件覆盖确定性策略；硬限制由数据完整性、执行、组合和风险引擎负责。
- 任何模型覆盖只允许降低风险，不允许超过策略原始风险预算或增加杠杆。

## 冻结候选：`calibrated_strategy_primary_v1`

1. 策略分必须为正，Council 不得存在来自确定性硬风险的 veto。
2. 没有可用点时统计模型时，使用纯策略的风险上限仓位；原始 LLM 概率和 Reviewer 主观意见只作为说明，不改变仓位。
3. 当 Forecast 中存在统计模型、统计权重不低于 0.25，且统计模型的 `down_probability - up_probability >= 0.15` 时，将策略仓位减半。
4. 其他情况保持策略仓位；不得因看多而超过策略仓位。
5. 数据源降级、执行价格不可用或确定性风险引擎阻断时仍不得生成可执行订单。历史盲测中的归一化价格不是实时订单，因此不构成该候选的执行阻断。

## 评估

- 与 `strategy_only`、`current_strict`、V1 三个候选同成本、同整手、同路径资本比较。
- 分别报告有模型与无模型回合；不得把“无模型时等于纯策略”宣传成模型增益。
- 报告统计模型概率的 Brier、准确率、盈利/亏损回合概率差和实际减仓回合。
- 首个 2026 年回合只作为小样本前瞻式历史挑战；少于 12 个新回合不晋级。
- Replay #7 只能用于结构诊断和反事实解释，不能成为 V2 的确认性证据。
- 即使通过，也只能进入冻结的前瞻影子账户，不自动改变正式手工下单建议。
