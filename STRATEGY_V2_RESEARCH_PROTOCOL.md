# Adaptive ETF V2 研究协议

登记日期：2026-07-14。该协议在首次运行 V2 历史比较之前冻结。

## 研究边界

`adaptive_v2` 是独立的前瞻挑战者，不替换 `adaptive_v1`，也不改写已经完成的锁定历史留出结果。由于 2015—2026 年 ETF 数据已经被本项目多次研究，V2 在这段数据上的任何结果都只能称为回顾性探索，不能作为新的确认性证据或策略准入依据。

V2 默认只能通过显式选择运行，并始终输出 `research_only=true`。在达到前瞻门槛前，它不能提高真实资金预算、不能自动生成券商订单，也不能覆盖现役策略。

## 固定结构

V2 只增加具有事前金融逻辑的三类能力：

1. 相关性去重：先按风险调整动量排序，再惩罚与已入选 ETF 的正相关重复暴露；
2. 多元风险配权：对 60 日协方差做 50% 对角收缩，使用只做多的最大分散化权重，并限制单个卫星仓位；
3. 连续状态控制：将市场宽度、20/120 日波动率冲击、60 日市场回撤分别映射到 0—1，再取最保守倍率；核心仓位随倍率在 35%—75% 之间连续变化，剩余风险预算转入防御 ETF。

其余规则沿用 V1：20/60/120 日风险调整动量、120 日绝对动量与均线过滤、防御 ETF 不参与进攻排名、目标波动率只降风险不加杠杆，以及排名缓冲降低边缘换手。

## 固定默认参数

| 参数 | 固定值 |
|---|---:|
| top_k | 3 |
| 动量权重 | 50% / 30% / 20% |
| 波动率/协方差窗口 | 60 日 |
| 协方差收缩 | 50% |
| 相关性惩罚 | 0.35 |
| 单卫星权重上限 | 60% |
| 宽度风险关闭/开启 | 25% / 75% |
| 波动率冲击警戒/停止 | 1.25 / 2.00 |
| 回撤警戒/停止 | -5% / -15% |
| 核心仓位下限/上限 | 35% / 75% |
| 卫星目标波动率 | 12% |
| 最小再平衡权重偏离 | 2% |

历史探索运行前不得根据结果修改这些参数。任何参数变化都必须登记为新的候选版本。

## 评估顺序

1. 先用合成数据验证相关性去重、协方差退化回退、连续状态缩放、动态核心仓位和仓位上限；
2. 再运行 2015—2022 开发区间，仅用于确认实现行为和识别明显失败；
3. 2023—2026 区间可以展示，但必须标记为“已污染的回顾性诊断”，不得形成准入结论；
4. 从登记后的第一个可用交易日开始建立独立前瞻影子账户。

## 前瞻门槛

- 60 个交易日：只判断运行稳定性、数据完整性和交易可执行性；
- 126 个交易日：允许形成 preliminary 结果，但不得晋级；
- 至少 252 个交易日：才允许与 ETF 等权、现役轮动和 V1 按同成本比较；
- 晋级仍需通过收益/Sharpe/回撤、配对移动块 bootstrap、多重试验修正和 2 倍成本压力；
- 如果 V2 失败，保留失败报告，不能在同一前瞻区间改参数后重新计算为独立证据。

## 允许声明

当前只允许声明：系统已经具备相关性去重、协方差风险预算、连续市场状态控制和动态核心仓位的可运行研究实现。

当前不允许声明：V2 已证明能跑赢 ETF 等权、已提高赚钱概率、已通过策略准入或适合真实资金。

## V2.1 执行修订记录

首次 V2.0 回顾性诊断显示，动态核心仓位使开发期和复用历史区间的成交次数均高于 V1。该现象不构成收益调参依据，但暴露了最低佣金下的小额再平衡问题。因此登记 V2.1：只有目标仓位与当前仓位相差至少组合净值的 2% 时才下再平衡订单。

V2.1 是新的执行版本。V2.0 历史结果不能继续作为 V2.1 的独立证据；V2.1 的任何历史结果仍只能是回顾性探索，前瞻计数从 V2.1 登记后的首个可用交易日重新开始。
