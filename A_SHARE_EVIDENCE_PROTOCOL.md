# A股能力与证据协议

## 目标

系统分别回答四个问题，不能用一份结果互相替代：

1. 用户冻结股票池内的统一排名是否优于简单规则；
2. 历史全市场的点时分层样本中，排名是否产生稳定超额；
3. A股真实成交约束下，结果能否形成可执行的前瞻组合；
4. 多 Agent 和统计学习模型是否在未见样本上提供增量价值。

证据合格只表示数据、时间切分和对照设计达到要求，不等于策略准入，更不等于收益承诺。

## 历史证券主数据与点时股票池

- 沪深交易所当前上市、上市日期、终止上市和退市日期组成版本化证券主数据；
- BaoStock `query_all_stock(day)` 组成指定历史交易日的真实证券集合和当日交易状态；
- BaoStock 日线提供历史 `tradestatus`、`isST`、原始价和复权价；
- 交易所主数据与每个 BaoStock 快照逐日做 Jaccard 交叉验证；任一回放截面低于 0.90，全部学习样本禁止训练；
- 已知证券代码变更进入显式、可审阅的别名注册表。当前 `sz300114 → sz302132` 被视为同一证券身份，避免旧代码截断造成伪退市损失；
- 强制重抓历史快照时执行整日原子替换，不能残留已删除或已规范化前的“幽灵成员”。

这套主数据用于身份和完整性审计。策略抽样只依赖当时股票池、当日交易状态和固定 seed，不使用未来收益、未来退市状态或未来财务数据。

## 全市场点时分层回放

`stock-market-replay` 在每个信号日执行以下协议：

1. 获取当日真实A股证券集合；
2. 排除同日不可交易和同日 ST/退市整理名称；
3. 按沪主板、深主板、科创板、创业板做确定性比例分层抽样；
4. 只使用 T 日收盘前可得数据计算因子和排名；
5. T+1 首个可交易开盘买入，5或20个交易日后卖出；
6. 执行100股整手、最低佣金、印花税、过户费、滑点、停牌和一字涨跌停；
7. 若证券在持有期内终止历史，使用最后可卖收盘价；完全无可回收价格时按零回收；
8. 用沪深300指数构造同暴露理论基准。指数不伪装成可成交ETF，不收交易费。

回放同时比较：

- 系统第一名与简单20日动量、同15%暴露沪深300；
- 分散Top-K与候选池等权、同多标的暴露沪深300；
- 排名IC、第一名命中率、累计收益、胜率、回撤、配对Bootstrap区间。

分层样本不是每天穷举全部约5000只股票，报告必须保留这条边界。

## 证据与策略双门禁

证据门禁要求：

- 至少30个非重叠回合；
- 所有股票池快照来源均为 BaoStock；
- 每个快照与交易所主数据 Jaccard 均不低于0.90；
- 历史ST、停牌和退市前价格链路可用。

策略门禁独立检查每个变体：

- 累计收益为正；
- 最大回撤不超过系统限制；
- 相对简单基准和同暴露沪深300的90%配对Bootstrap下界为正；
- 平均排名IC为正。

只有策略门禁通过，才可进入前瞻影子盘挑战；否则状态固定为 `research_only`，前端不得显示为实盘推荐。

## 当前真实结果（Replay #7）

2024-01-01至2025-12-31、5日持有、30回合、每回合40只点时分层样本：

- 数据证据：`measured`，30/30回合；平均Jaccard 99.9994%，最小Jaccard 99.9806%；
- 系统第一名：+6.88%，最大回撤-5.90%；同15%暴露沪深300为+1.41%；
- 第一名相对简单动量的90%区间为正，但相对沪深300区间仍跨零；
- 分散Top-3：-3.41%，最大回撤-15.81%；同45%暴露沪深300为+4.21%；
- 平均排名IC为-0.0992；历史ST排除14次；随机分层样本中没有抽到最终退市股；
- 结论：数据证据合格，但两个策略变体均未通过准入，状态为 `research_only`。

完整产物：`data/reports/stock-market-replay-7.md` 和 `.json`。

## 学习样本治理

- 固定当前股票池回放仍标记 `training_eligible=false`，因为存在用户选择和幸存者偏差；
- 全市场点时分层回放只有在每个快照完整性通过后才标记 `training_eligible=true`；
- 截至本次回放，已形成1,136条可训练的5日点时市场样本；最新候选在511条滚动验证观测上的 Brier 为0.2056（基线0.2044），Log Loss为1.0160（基线1.0042），三折通过率为0%，因此被拒绝激活；
- 20日点时市场样本仍不足，不能借用固定池偏差样本凑数；
- 未来真实预测到期后进入同一冠军—挑战者、漂移监控和事件关联流程。

## 点时财务与估值

年度财报采用保守可用时间：上一年度年报从次年5月起才允许进入历史特征，避免发布日期未知时的财务泄漏。

估值由确定性代码生成。标准化EPS、自由现金流/股和每股净资产分别形成区间；每个输入、折现假设和缺失字段进入审计包。LLM只负责解释、质疑和寻找反证，不得补造现金流或估值数字。

## 前瞻影子账户

系统继续维护三类独立A股账户：

- `stock_radar_equal_weight`：冻结股票池等权基准；
- `stock_top_rank_shadow`：确定性第一名研究账户；
- `stock_full_system_shadow`：同日完整多 Agent 审核通过后才允许新增仓位。

账户信号、订单、成交和净值快照不可回写。真实自动券商执行仍禁用，只生成手工下单辅助信息。
