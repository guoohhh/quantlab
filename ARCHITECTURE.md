# QuantLab 第一版统一架构

## A股点时市场与策略权限边界（2026-07-14）

A股历史研究现在分为证券事实层、点时特征层、策略证据层和部署门禁层。交易所主数据负责上市/退市日期交叉校验，BaoStock 负责指定历史日真实证券集合、`tradestatus`、`isST` 和退市前行情；显式证券别名注册表保证代码变更前后是同一资产身份。任一回放截面的主数据 Jaccard 低于0.90，整批样本禁止训练。

市场级回放只抽取当时股票池的确定性分层样本，不使用未来退市状态。沪深300使用不可交易指数比较器并分别匹配单标的和多标的风险暴露，避免ETF历史缺失或仓位差制造虚假Alpha。

证据资格和策略部署是两个独立状态。30回合、点时来源和完整性可以使证据达到 `measured`，但收益、回撤、同暴露Bootstrap和排名IC任何一项失败，策略仍为 `research_only`；统计模型也只有在滚动 Brier 与 Log Loss 同时优于基线后才能激活。

## ETF 决策权限边界（2026-07-14）

ETF 与个股不再共用同一种 LLM 硬否决语义。ETF 的技术、动量、结构风险、组合风险和宏观 Agent 仍全部运行，但结构风险与组合风险输出是战略评分，不拥有文本式硬 veto；最终硬限制由数据质量、成本、整手、仓位上限、组合与执行引擎确定。股票和可转债的确定性永久损失/偿付风险否决保持不变。

ETF V2 闸门的权限顺序是：确定性策略产生风险预算内信号 → Agent 解释与反证 → 点时统计模型提供校准概率 → 只有已被预测器接纳且明确看空的统计模型可以减半仓位。原始 LLM 和 Reviewer 不能增加仓位，也不能凭未写入规则的主观条件覆盖策略。历史盲测的归一化价格只用于隐藏身份，`entry_price` 必须为空。

## 1. 产品决策

1. 第一版同时实现 ETF 轮动、A 股反转、可转债双低，但每条策略都可独立关闭。
2. 第一版包含多 Agent 和 LLM 概率预测；LLM 不直接绕过风控生成订单。
3. 用户资金按 10 万元以内设计，只生成手工下单清单。
4. 资金比例不写死。策略先获得独立风险预算，再按市场状态、近期样本外表现和置信度动态调整。
5. 分数凯利默认 1/4，可配置为 0 到 1；未经 walk-forward 校准时禁止高于 1/2。
6. 免费数据源优先。任何降级、缺失、陈旧数据必须进入最终报告，禁止静默使用示例数据冒充真实数据。

## 2. 总体流程

```text
数据源 -> 标准化/PIT -> 特征与策略 -> 独立回测/校准
     -> 市场状态 -> 动态策略预算 -> 多 Agent 研究与概率预测
     -> 硬风控闸门 -> 组合经理 -> 审计 Agent -> 手工下单清单
     -> 事后 1/5/20/60 日归因与模型校准
```

## 3. 多 Agent 设计

| Agent | 作用 | 是否可影响仓位 |
|---|---|---|
| Quant Analyst | 解释因子、策略、回测与市场状态 | 只能提交量化证据 |
| Fundamental Analyst | 财务质量、估值、退市与事件风险 | 可触发风险建议 |
| News Analyst | 新闻、公告、政策与情绪 | 只能提交带来源证据 |
| Bull Researcher | 最强看多论证 | 否 |
| Bear Researcher | 最强反证与失败路径 | 否 |
| Forecast Agent | 输出 5/20 日概率分布和失效条件 | 作为软信号 |
| Risk Officer | 检查硬约束、数据质量和集中度 | 拥有否决权 |
| Portfolio Manager | 在允许区间内生成目标权重 | 不能突破硬约束 |
| Reviewer | 检查证据、矛盾、降级和合规 | 可打回，不可放宽风控 |

多 Agent 采用显式共享状态和审计事件。Agent 输出使用 Pydantic Schema；解析失败时降级为 `hold/review_required`，不猜测字段。

## 4. LLM 预测定义

预测不是单一“涨/跌”，而是：

- `horizon_days`: 5 或 20 个交易日
- `up_probability / flat_probability / down_probability`
- `expected_return_pct` 与合理区间
- `confidence`：证据覆盖率 × 多空一致性 × 数据质量 × 校准系数
- `drivers`、`counter_evidence`、`invalidation_conditions`
- `as_of` 与所用数据版本

LLM 概率在积累足够历史样本前只做软信号。事后使用 Brier Score、分桶命中率和校准曲线调整其权重。

## 5. 策略与动态资金分配

ETF 策略分为现役基线和研究候选。研究候选使用风险调整双动量、绝对趋势过滤、防御资产隔离、逆波动率、市场宽度切换与目标波动率，但只能通过 `STRATEGY_RESEARCH_PROTOCOL.md` 定义的开发期/锁定留出流程进入前瞻观察。策略实验室固定 `research_only=true` 和 `formal_strategy_changed=false`，不能修改现役配置、预算或订单。

V2 是与 V1 并存的前瞻挑战者，不覆盖 V1 源码和失败留出。它用相关性惩罚避免卫星 ETF 重复暴露，用 50% 对角收缩协方差和只做多最大分散化代替单纯逆波动率，并将市场宽度、短长波动率冲击和风险资产组合回撤映射成连续风险倍率。核心仓位在 35%—75% 之间随状态变化，目标偏离不足组合净值 2% 时不做小额再平衡。历史比较统一标记为 `retrospective_exploratory_only`，只有登记后的独立前瞻影子账户能形成准入证据。

### ETF 轮动

默认池覆盖大盘、成长、红利、黄金、海外与国债。使用多周期动量、波动率和趋势过滤；国债 ETF 既是候选资产也是风险关闭信号。

### A 股反转

从高流动性非 ST 股票中选择中短期跌幅靠前、但财务与交易风险过滤合格的股票。做行业/市值中性化，避免把小盘暴露误认为 alpha。

### 可转债双低

按价格与转股溢价的标准化组合排序，叠加剩余规模、评级、到期收益率、强赎与流动性过滤。

### 动态预算

策略得分由以下部分构成：

```text
样本外 Sharpe + Calmar + 近期有效性 + 市场状态适配
- 回撤惩罚 - 换手成本 - 数据质量惩罚
```

通过 softmax 转换为预算，并施加单策略上下限。预算变化设置平滑和最小调仓阈值，避免追涨杀跌。

当前实现已将 ETF walk-forward 结果注册到 SQLite，并与预测模型注册表严格分离。策略必须同时通过绝对样本外门槛，以及相对 ETF 等权基准的收益/Sharpe/回撤门槛，才解除低预算限制。当前 ETF 轮动未通过相对基准闸门，因此只获得低探索预算。预算平滑仍是下一阶段工作，当前依靠单策略上下限、单标的上限、整手约束和最小交易金额控制跳变。

## 6. 回测原则

- 信号在 T 日收盘形成，默认 T+1 开盘成交。
- 财报按实际披露日进入可见集；数据必须携带 `available_at`。
- 股票买入数量当日冻结，下一交易日才可卖出。
- ETF 是否 T+0 由标的元数据决定，不统一套用股票规则。
- 涨跌停、停牌、整手、最低佣金、印花税、过户费和滑点逐笔处理。
- 所有参数必须支持 walk-forward；报告同时给出样本内、样本外和敏感性结果。

ETF 轮动已实现滚动训练/测试、逐折训练窗选参、测试窗独立资金和参数网格敏感性。股票与可转债必须先解决点时股票池和可靠历史风险字段，才允许注册同等级验证。

## Agent 职责与路由

多 Agent 不是让多个模型重复回答同一个问题，而是职责、输入、输出和权限彼此隔离。

### 战术委员会

| Agent | 权重/模式 | 唯一职责 |
|---|---:|---|
| Technical | 1.3，投票 | 日/周/月价格结构、趋势和量价确认 |
| Momentum | 1.5，投票 | 动量加速度、路径质量、量价非对称、回撤反转 |
| Value Veto | 0.6，仅否决 | 极端估值、偿债和安全边际风险 |
| Risk | 1.0，仅否决 | 永久损失、流动性、集中度、数据质量、交易可行性 |
| Macro | 0.7，投票 | 市场状态、流动性和策略环境适配 |

### 长期质量委员会

股票且存在经过验证的财务数据时，才启用以下 Agent：

- Buffett：护城河、所有者收益、定价权、管理层诚信和安全边际。
- Munger：逆向思考、激励机制、认知偏误和永久损失路径。
- Graham：盈利稳定、资产负债表、稀释和保守估值。
- Fisher：长期成长、研发效率、管理层执行和组织深度。
- Lynch：公司类型、增长与估值匹配、现金流和可解释故事。

战术得分与长期得分分开计算，再按 65%/35% 融合。任何确定性风险闸门或合理的 veto-only Agent 否决都会将新增目标仓位降为 0，并要求人工复核。

当日 Agent 结论也进入组合计划，但权限是单向的：只能降低目标上限、阻止新增、要求复核或建议减仓，不能把确定性策略和风险层不允许的仓位放大。没有当日研究的标的不假装已经通过 Agent 审核。

## 探索研究层：专家圆桌

圆桌会议与正式委员会分离。用户必须先选择一份已经保存的研究报告，系统冻结该报告的价格、因子、财务、新闻、Agent 结论、预测和确定性决策轨迹，再让用户自选 2–8 位大师或专家进行 1–3 轮讨论。

- 同一轮角色并行发言，避免固定顺序造成观点锚定；
- 第二轮起必须回应此前最强论点，并标记是否改变观点；
- 每次发言输出证据路径、证据缺口和追问；
- 主持人只归纳共识、分歧、多空最强论点和下一步研究；
- 圆桌结构化结果固定 `formal_decision_changed=false`，不能修改正式仓位、风控或订单；
- 单角色失败只降级该发言，主持人失败时生成保守结构化摘要并保留审计。

## IronQ 因子层

当前因子引擎包含 20/60 日动量、动量加速度、60 日路径质量、20 日量价非对称、60 日收益偏度、60 日价格位置、5/20 均线乖离、RSI、日周月共识和回撤反转四条件。

因子权重在完成滚动 IC 校准前属于透明的先验权重，不能宣称为最优参数。系统已提供横截面 Rank IC/Pearson IC 计算，下一阶段用训练窗口内 IC 经收缩后驱动测试窗口权重。

## 多候选擂台与组合压力测试

市场雷达先按点时强弱筛出2–6个 ETF；每个候选随后独立运行完整多 Agent 委员会和5/20日预测。擂台分将确定性决策综合分、两周期概率方向、因子分、雷达强弱和最终动作映射到统一量纲，同时对人工复核、veto、Reviewer拒绝与数据降级施加惩罚。

横向排名不拥有放宽权限：只有通过 Reviewer、未触发 veto、无需人工复核且动作为 `buy/add/watch/hold` 的候选，才有资格进入 shortlist。随后使用最多252日对齐后复权收益做正相关去重；负相关不会被当作重复暴露。

入围标的会形成一个总暴露30%的等权假设组合，仅用于比较历史波动、VaR、CVaR、最大回撤、方差贡献和四类透明压力情景。它不会生成订单。实际仓位仍由组合规划器根据策略准入、风险预算、整手与资金约束产生。

每个擂台冻结当日排名、原雷达排名和 shortlist。5/20个交易日成熟后，学习周期用后复权收盘价结算真实收益，比较 Agent 第一名、原雷达第一名、全候选等权、shortlist 等权和事后最佳候选，并保存排名信息系数与 regret。该成绩单是未来学习排名权重的监督标签；第一版在满30个完整样本前不自动调权，避免小样本自我强化。

## A股股票发现与用户自选

股票路径分成三个成本层级：

1. 名称/代码搜索与 SQLite 自选池，只管理用户关注范围；
2. 最多20只股票的批量快速筛选，一次获取行情后计算因子、多时间框架、流动性、回撤形态、财务快照和相关性，不调用 LLM；
3. 用户明确选择最多5只股票后，才运行完整多 Agent 与投资大师深度会诊。

系统推荐不是单一路径排行榜。趋势质量、价值质量、成长质量、回撤修复和高股息五种风格并行返回候选，按轮询方式合并，避免代码顺序让某一风格占满名额；随后使用统一评分公式重排。公式包含因子综合、多周期趋势、动量路径、财务快照、流动性、回撤设置和风险调整。高波动、20日过度延伸及 ST 标的会被惩罚或阻止入围。所有输出都标记为研究候选，真正仓位仍经过深度会诊和组合规划器。

固定股票池仍可运行独立点时回放，但其样本因用户选择和幸存者偏差默认禁训。市场级回放使用 BaoStock 指定日期真实证券集合、历史ST/停牌状态、退市前行情和交易所主数据交叉验证，再按板块确定性分层抽样；它可以回答“历史市场分层样本中的排名是否有效”，但仍不等同于每日穷举全市场。

股票深研中的估值由确定性财务模块生成。标准化EPS、自由现金流/股和每股净资产分别形成方法区间，再汇总为保守下沿、中枢和上沿；每个输入与折现假设进入审计包。LLM只能解释和反驳这些结果，不能自行创造DCF数字。

## 持续学习闭环

系统将 LLM 视为一个可校准信号源，而不是不可审计的自修改模型。每次预测冻结特征和原始 LLM 概率；结果到期后回填实际收益，训练按资产域隔离的 5/20 日 Softmax 概率模型。

模型准入必须通过时间外 Brier Score 与 Log Loss 双重基线。统计模型参与最终概率的权重由样本外改善幅度和验证样本量决定，最高 50%，不能覆盖风险否决。

新闻、财报和监管事件用于解释预测误差，但只输出证据关联，不声称因果。完整设计见 `LEARNING_SYSTEM.md`。

LLM 层支持同 Provider 多 Key、OpenAI/DeepSeek/本地开源模型跨 Provider 路由。每个端点具有独立熔断、重试、并发限制和 token/延迟审计；角色偏好不能绕过故障切换。单个 Agent 失败时输出零置信度降级结果，Reviewer 失败时强制人工复核。完整设计见 `LLM_ROUTING.md` 和 `PROMPT_GOVERNANCE.md`。

## 7. 技术选型

| 层 | 选择 |
|---|---|
| Python | 3.11 为最低版本，CI 同时测试 3.11/3.12 |
| 配置 | TOML + 环境变量 |
| 数据 | pandas + Parquet；SQLite 保存元数据与审计日志 |
| 编排 | LangGraph，可在无 LangGraph 时使用顺序执行器测试核心逻辑 |
| OpenAI | Responses API，结构化输出 |
| DeepSeek | OpenAI-compatible Chat Completions，JSON Schema 校验与重试 |
| UI | Typer CLI + Streamlit/Plotly |
| 测试 | pytest；核心金融计算使用确定性 fixture |

## 8. 不采用的做法

- 不直接照搬未经独立验证的黑客松权重或宣传胜率。
- 不让 LLM 计算技术指标、手续费、仓位上限等确定性数值。
- 不把 Markdown 表格解析作为数据接口的唯一契约。
- 不在数据失败时悄悄换成样例数据。
- 不把回测收益当作策略已有效；必须报告样本外与成本敏感性。

## 9. 前瞻证据与每日运行

系统维护五个ETF/基准账户，以及 `stock_radar_equal_weight`、`stock_top_rank_shadow`、`stock_full_system_shadow` 三个A股账户。信号在 T 日收盘冻结，只能在之后第一个真实交易日开盘撮合；A股账户额外执行T+1、停牌、一字涨跌停、100股整手、印花税和股票滑点。历史价格不能用于回填模拟盘起点。

`daily-cycle` 默认同时运行ETF模拟盘和配置中的A股固定研究池影子盘，再串联预测结果回填、模型漂移监控和今日投资摘要。步骤局部失败会记录为 `degraded`，其他成功步骤仍保留，不会伪装成全链路成功。
